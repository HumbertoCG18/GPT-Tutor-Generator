# MOTOR-9-01 — adendo ao plano de integração da PR #9 (25/09/2026)

Sessão Claude Code na nuvem, noturna, sem supervisão. Só diagnóstico: nenhum conflito resolvido,
nenhum código alterado, nenhum commit em `feat/motor-atribuicao`, `main` ou na PR #9, sem merge.
Delta sobre o plano de 24/09 (`docs/reports/2026-09-24-motor-9-01-plano-integracao.md`, PR #77),
feito com `main` `77a043f5` e motor `76e4357b`. O que o plano diz e continua válido não é repetido;
numeração de seções e decisões continua a do plano. Este arquivo também é o handoff da sessão (§9).

## 0. Resumo do delta

1. **12 conflitos** nas SHAs atuais (as duas direções): os 9 do plano mais `.claude/settings.json`,
   `.gitleaks.toml` e `src/builder/sources/m365.py`. Dos 9 antigos, 6 têm o hunk byte-idêntico ao do
   plano; 3 mudaram: `README.md` (1 linha nova da `main` fora do hunk, mesma resolução),
   `docs/reports/pendencias.md` (+77 linhas do motor, mesma resolução) e `scripts/hooks/pre-commit.sh`
   (a `main` reescreveu o passo 2: **a resolução muda** de "motor" para "main + 1 comentário do motor").
2. **`m365.py` conflita só no bloco de imports** (1 hunk, 5 linhas). O cofre do token (#83) e as
   quatro mudanças do motor caem em funções distintas e o git mescla o resto sozinho. Na árvore
   simulada, os dois comportamentos estão presentes e os testes dos dois lados passam (56 passed).
3. **Suíte e ratchet sem mudança de veredito**: conjuntos de falha idênticos entre a ponta do motor e a
   árvore mesclada (26 falhas + 2 erros de coleta, todos preexistentes); Ruff continua em 61 impressões
   digitais / 97 achados acima do baseline (D3 inalterada); contrato arquitetural verde; cobertura
   84,31 %.
4. **Bloqueio novo**: o check `secrets` (#83, obrigatório na `main`) reprovaria a PR #9 em dois passos:
   (A) `tests/fixtures/cg/Cronograma2026-2.pdf` versionado e ignorado por `*.pdf` (único resíduo após o
   merge; os 7 `.superpowers/**` do motor somem porque a `main` os apagou em #79) e (C) gitleaks com 2
   achados `generic-api-key` em commits antigos do motor, ambos falsos positivos (SHA-256 de arquivo num
   manifesto; CSS de HTML exportado do Word). Exceções estreitas validadas nesta sessão (D13, D14).
5. **gitleaks obrigatório na nuvem**: o instalador funciona com a rede do ambiente (1,2 s, SHA-256
   conferido, idempotente), mas exige estar em `<dir>/scripts/security/` — copiado para `/tmp` quebra na
   importação (`parents[2]`). O SessionStart da `main` já instalou 8.30.1 e o hook obrigatório nesta
   sessão, porque o clone nasce na `main`. Propostas para o script do ambiente e para o guia em §5.
6. **Decisões fechadas pelos fatos**: D4 (token fora do índice desde #79, revogado, cofre em #83;
   reescrita de histórico segue separada), D9 (continuação da #50 commitada em `15ea8c4a`, exatamente os
   5 arquivos do plano). `eebc698c` (M1) muda a referência do replay de preservação (§4.3).

## 1. Base fixada

| Referência | SHA | Data | Conferido |
|---|---|---|---|
| `origin/main` | `d815e205bac69bc6cf60623f63167052d617a203` | 24/09 (merge #83) | igual ao informado pelo usuário em 25/09 |
| `origin/feat/motor-atribuicao` | `2589ed5a95ab8dd841de384bd219d80ee27d2cbf` | 24/09 | igual |
| ancestral comum (`git merge-base`) | `8f1081927ecac16d658641fb39a8ab974c17238c` | 04/06 | inalterado |
| árvore `merge-tree main ← motor` | `e7779b38749d9b4b912cccfeb7115a61f0d63ddf` | — | gerada nesta sessão; `motor ← main` = `e4808da9a70877a2ebc36006745331a716b13306`, mesmos 12 conflitos |
| árvore do plano, recomputada | `b155f96484c537d25c69d0122842b840928a20d9` | — | `merge-tree 77a043f5 76e4357b`; difere da `5e7805a5` do plano só nos rótulos dos marcadores (a árvore do plano não persistiu neste clone) |

- Clone raso → `git fetch --unshallow`; Git 2.43.0. Divergência: 67 commits só na `main` (eram 62)
  e 1117 só no motor (eram 1112). Arquivos desde o ancestral: `main` 253 (159 A, 12 D, 21 M, 61 R;
  eram 239); motor 2577 (2354 A, 21 D, 135 M, 67 R; eram 2567).
- PR #9 em 25/09: aberta, não rascunho, `mergeable_state = dirty`, head `2589ed5a`, base registrada
  ainda `208af96e` (obsoleta), 1117 commits, 2577 arquivos, +987 237/−7 746. Checks da `main` no merge
  da #83 (`d815e205`): `core` e `secrets` SUCCESS (25/09 00:55 UTC).
- Ambiente: `pydantic` e `pytest` presentes; `/tmp/setup-nuvem.log` lido (script do ambiente roda como
  root em `/home/user`, instala pip, não instala hook). O hook `.git/hooks/pre-commit` já estava
  instalado e executável ao abrir a sessão, byte-idêntico ao `scripts/hooks/pre-commit.sh` da `main`
  (#83), com `gitleaks` 8.30.1 em `~/.local/bin`: é o SessionStart da `main` (`setup_nuvem.sh`, com
  `CLAUDE_CODE_REMOTE=true`), que rodou porque o clone abre na `main` antes de a sessão trocar de branch.
  Na branch desta sessão (base motor) não existe `scripts/security/`, então esse hook cai no ramo
  "gitleaks direto" (falha sem o binário; com ele, varre o stage com o `.gitleaks.toml` do motor).

## 2. Commits novos desde o plano

### 2.1 `main` (5 commits, PRs #79 e #83)

| Commit | PR / issue | Conteúdo | Efeito na integração |
|---|---|---|---|
| `2ce46099` (merge `345e423f`) | #79, Refs #9 | remove do índice, sem reescrever histórico, 14 arquivos ignorados: `moddle/.m365_token.json`, `.token-savior-cache.json`, `.claude/settings.local.json`, 11 em `.superpowers/brainstorm/**` | o merge deixa de trazer o token e os brainstorms ao motor; a deleção da `main` vence os 7 brainstorms que o motor ainda versiona (o motor não os alterou) |
| `3688ac96` | #83, Refs #80 | `src/utils/credential_store.py` (novo, 126 linhas: diretório por usuário, DPAPI via ctypes, escrita atômica, recusa destino dentro do repositório), `src/builder/sources/m365.py` (+50/−16), `tests/test_credential_store.py` (261), `tests/test_m365.py` (+33/−?), `README.md` (1 linha) | conflito 12 (`m365.py`, só imports); `tests/test_m365.py` auto-mesclado |
| `72df8348` | #83, Closes #81 #82 | `.gitleaks.toml` (+11: regra `oauth-refresh-access-token`), `scripts/security/{gitleaks_scan,check_repo_hygiene}.py`, `scripts/security/setup_nuvem.sh`, `.github/workflows/security.yml` (check `secrets`: A versionado-ignorado, B caminho de credencial, C gitleaks por commit, D testes), `scripts/hooks/pre-commit.sh` (+31/−8, obrigatório), `.claude/settings.json` (+11, SessionStart), `.mex/context/setup.md` (+21), `tests/test_security_guards.py` (349) | conflitos 10, 11 e o 3 (pre-commit) mudam; check `secrets` passa a valer para a PR #9 (§6.2) |

`.github/python-quality-baseline.json`, `scripts/verify_python_quality.py` e `python-quality.yml`
não mudaram desde o plano. Os arquivos novos da `main` têm 0 achados de Ruff.

### 2.2 motor (5 commits)

| Commit | Conteúdo | Código tocado | Efeito na integração |
|---|---|---|---|
| `15ea8c4a` fix(timeline), Refs #50 | data da prova pela linha da prova; `EXAM_INDEX.md` sempre regenerado | `ops/bootstrap_ops.py`, `ops/pedagogical_regeneration.py`, `timeline/index.py`, `tests/test_assessment_label_and_plan_section.py`, `tests/test_pedagogical_regeneration_order.py` (5 arquivos, +93/−7) | é a continuação local da #50 do plano (§3.5), agora publicada (D9, §4.2); a `main` não toca esses arquivos |
| `10f4b23d` docs(motor) W-AB | harness `unidade_caminhos_24-09.*`, `wab_unidade_fallback_fronteiras_24-09.*`; tracker (+48) | nenhum | só o conflito 9 cresce |
| `eebc698c` feat(motor) M1 | texto confiante vence o bloco do resolvedor antigo na unidade | `routing/file_map.py` (+12: kwarg `block_is_fallback`), `routing/resolver_apply.py` (+5/−2), `ui/dialogs.py` (+11), `docs/Overview-Sistema.html` (1), testes +5 em `test_core.py`, `test_reconcile_unit_block.py`, `test_resolver_apply_units.py`; harness `wab_verifica_m1_src_24-09.*`; tracker | `dialogs.py` também é tocado pela `main` (#40), mas continua auto-mesclado sem conflito; muda a referência do replay (§4.3) |
| `3c8f26d9` docs(motor) W-AC e teto | `2026-09-24-teto-regime-cru.md`, harness W-AC, tracker | nenhum | — |
| `2589ed5a` docs(motor) VOCAB | `2026-09-24-regime-vocab-desenho.md`, tracker | nenhum | — |

## 3. Conflitos (12)

### 3.1 Novos (3) — mesmas colunas do §4 do plano ("ours" = `main`, "theirs" = motor)

| # | Arquivo | Tipo | O que a `main` mudou | O que o motor mudou | Comportamento a preservar | Resolução proposta | Evidência | Teste que demonstra | Decisão pendente |
|---|---|---|---|---|---|---|---|---|---|
| 10 | `.claude/settings.json` | conteúdo (2 hunks no mesmo array `hooks`) | bloco `SessionStart` que roda `scripts/security/setup_nuvem.sh` (`72df8348`) | nada desde o plano: byte-idêntico à `main` de `77a043f5`. Conflita porque os dois lados mudaram o arquivo desde o ancestral e o git não alinha o fechamento do array | guardas PreToolUse (fachada do engine e anti-padrões Gemini) nos dois; SessionStart na nuvem | Versão da `main` (superconjunto: os dois hunks são só o bloco novo) | `diff main@77a043f5 motor@2589ed5a` vazio; `diff 77a043f5..d815e205` = +11 | `json.load` na cópia mesclada; nesta sessão o SessionStart imprimiu "[nuvem] pre-commit instalado; gitleaks: 8.30.1" | nenhuma (a nuvem não edita este arquivo; a resolução é do usuário na integração) |
| 11 | `.gitleaks.toml` | add/add (1 hunk) | regra `oauth-refresh-access-token` (JSON/YAML/atribuição, entropia mínima, +11 linhas) | nada desde o plano: idêntico à `main` de `77a043f5` (allowlist de SHA de commit) | regra nova + allowlist de SHA | Versão da `main`, mais as duas exceções estreitas de D14 | gitleaks com a regra da `main` sobre `MB..main` acusa `moddle/.m365_token.json` no commit `40cfa28e` (regra funciona) | `tests/test_security_guards.py` (23 passed na cópia, inclusive com D14 aplicada) | **D14**: exceções para os 2 falsos positivos do histórico do motor (§6.2) |
| 12 | `src/builder/sources/m365.py` | conteúdo (1 hunk, linhas 15–19: imports) | `3688ac96`: `import os`; `from src.utils import credential_store`; `_SECRET_NAME`, `ENV_NO_PERSIST`, `LEGACY_TOKEN_PATH`; `_sem_persistencia`, `_avisar_cache_legado`; `_save_token`/`load_cached_token` pelo cofre; `get_client` falha antes do device login sem cofre (mensagem com a instrução nos 160 primeiros caracteres); remove `_token_path`; docstring do módulo | `8cb6f23e`, `e0dee708`, `44d7a4fe`, `00046da3`: remove `import re`, `import unicodedata`, `_DEFAULT_ALIASES`, `_norm_tokens`, `_token_affinity`, `match_card`; adiciona `filter_in_path`, `effective_m365_filter`, import `refresh_card_listings_from_disk`; `download_subject_m365(client, m365_filter, section_index, …)` com `warnings`, `mapping` (basename, card, origem) e reconciliação com o disco | os dois: token fora do repositório (cofre; `GPT_TUTOR_M365_NO_PERSIST=1`; cache legado só avisa) **e** card só pelo `section_index` da API do Moodle, sem match léxico, filtro efetivo digitado > salvo, aviso alto quando vazio | Editar o hunk: manter só `import os` (a `main` conservou `import re` porque nunca o removeu; o motor o removeu junto com o match léxico; `re` e `unicodedata` não são usados no arquivo mesclado; `os` é usado por `_sem_persistencia`). Todo o resto o git mescla sozinho: as funções da `main` (linhas 123–215 da árvore mesclada) e as do motor (58–80, 220–310) não se sobrepõem | árvore `e7779b38`: só `os.environ` em uso; `ast.parse` ok; símbolos dos dois lados presentes; Ruff sem achado no arquivo | `tests/test_m365.py` auto-mesclado (testes do cofre da `main` + testes de `download_subject_m365` do motor) e `tests/test_credential_store.py`: 56 passed, 1 skipped (DPAPI real só no Windows) na cópia | nenhuma. Observação: fora do Windows `get_client` exige `GPT_TUTOR_M365_NO_PERSIST=1` (comportamento da `main`, já documentado no `setup.md`) |

### 3.2 Os 9 antigos — reconferidos

Método: `merge-tree` recomputado nas SHAs do plano (`b155f964`) e comparado hunk a hunk com a
árvore atual (`e7779b38`), com os rótulos dos marcadores normalizados.

| # | Arquivo | Hunk | O que mudou desde o plano | Resolução |
|---|---|---|---|---|
| 1 | `pyproject.toml` | idêntico | nada | a do plano (união; `py311`; D2) |
| 2 | `.gitignore` | idêntico | nada nos dois lados; `diff main → motor` continua sendo só `/.alethe/` e `/%SystemDrive%/` | versão do motor **mais a negação de D13** (`!tests/fixtures/cg/Cronograma2026-2.pdf`), se aprovada |
| 3 | `scripts/hooks/pre-commit.sh` | **mudou** (2 hunks) | `main` (`72df8348`) reescreveu o passo 2: obrigatório; com `scripts/security/` presente chama `check_repo_hygiene.py staged` e `gitleaks_scan.py staged`; sem ele, ramo "gitleaks direto" que falha sem o binário; sai o modo "só avisa" e a dica `winget`. Motor: texto antigo + comentário do `chmod +x` (`6d778f39`) + dica de instalação fora do Windows (que a `main` removeu junto com o modo aviso) | **muda**: hunk 1 (comentário do `chmod`) = motor; hunk 2 (corpo do passo 2) = `main`. Assim montado, num repositório temporário o hook aceitou um commit limpo e bloqueou um commit com token sintético; `sh -n` ok; `tests/test_security_guards.py` verde com ele |
| 4 | `.workflow/pendencias_workflow.md` | idêntico | nada | `main` (D6) |
| 5 | `.mex/patterns/engenharia-produto-details.md` | idêntico | nada | `main` |
| 6 | `AGENTS.md` | idêntico | nada | motor |
| 7 | `.mex/ROUTER.md` | idêntico | nada | motor |
| 8 | `README.md` | hunk idêntico; 1 linha da `main` fora dele | `3688ac96` reescreve a linha 227 (OneDrive/M365: token fora do repositório, DPAPI). O motor não mudou | `main` inteira (inclui a linha nova) |
| 9 | `docs/reports/pendencias.md` | **mudou** (+77/−1 do motor) | `10f4b23d`, `eebc698c`, `3c8f26d9`, `2589ed5a`: MOTOR-9-01 "concluída (24/09)", MOTOR-9-02 "pausada", blocos "Medido" W-AB e caminhos da unidade, "Publicado" teto e W-AC, "Planejado" VOCAB. A `main` não mudou | a do plano (base motor + entradas da `main`; D5 continua: o arquivo pode ter edições locais) |

### 3.3 Auto-mesclados que merecem leitura

- `.mex/context/setup.md`: sem conflito, mas o resultado se contradiz. A linha 74 (motor) diz "O
  `gitleaks` também fica ausente; o hook só avisa"; a seção nova da `main` (linhas 121–127) diz "Sem
  gitleaks 8.30.1 ou sem Python 3.8+, o commit falha; não há modo de aviso". Idem
  `.mex/context/sessao-nuvem.md` §2 (só no motor, sem conflito): "sem gitleaks ele só avisa". D15.
- `tests/test_m365.py`: 3 blocos da `main` (cofre) entre os testes do motor; 22 testes, verdes.
- `src/ui/dialogs.py`: agora com três fontes (motor #47/#48/#63/#65/M1, `main` #40) e ainda sem
  conflito; os 40 dirigidos da `main` (§5.1 do plano) continuam verdes sobre ele.

## 4. Decisões reavaliadas com os fatos novos

### 4.1 D4 — token e arquivos ignorados versionados na `main`

Fechada pelos fatos. Na ponta da `main` não há caminho de credencial nem arquivo versionado-ignorado
(`check_repo_hygiene.py tree` equivalente: 0), o refresh token foi revogado (informado pelo usuário;
o commit `2ce46099` registra 24/09 16:00) e o cofre da #83 substitui o cache em texto puro. O merge
já não traz o token ao motor. O que resta, fora da PR #9: o arquivo continua no histórico (commits
`40cfa28e` e `4bec7e97`, apontados por `check_repo_hygiene.py range motor..main`) e na ponta do branch
remoto `origin/docs/14-c6-quality` (único remoto que ainda o versiona). Reescrever histórico continua
decisão separada; o passo 2 da sequência do §7 sai.

### 4.2 D9 — continuação local da #50

Fechada: `15ea8c4a` (pai `76e4357b`, sobre `36b00ab9`) contém exatamente os 5 arquivos do §3.5 do plano
(`bootstrap_ops.py`, `pedagogical_regeneration.py`, `timeline/index.py`,
`test_assessment_label_and_plan_section.py`, `test_pedagogical_regeneration_order.py`; +93/−7, igual ao
diff local descrito). A `main` não toca nenhum deles desde o ancestral, logo entram na PR #9 sem
conflito. O passo 1 da sequência fica reduzido ao tracker local. A descrição da PR #9 ainda diz
"continuação da #50 ... não commitada": corrigir junto com a #49 (D8).

### 4.3 `eebc698c` (M1) — o que muda na validação do §8

M1 não é da integração (é entrega do motor com Gate próprio, replay 350/350 contra o braço congelado)
mas muda as referências do §8 do plano:

- **§8.1, baselines**: motor `2589ed5a` Windows = 2448 passed + 1 falha preexistente (golden FR),
  segundo a mensagem do commit; Linux nuvem = 26 F / 2354 P / 30 S / 1 D / 2 E (esta sessão, §6.1).
- **§8.5, replay integral**: a captura congelada de 23/09 (`preservacao_base_v2_65_23-09`, unidade
  248/284) deixou de ser a identidade da ponta. Por desenho, a ponta difere dela em exatamente 1
  material (CG `exercicios-sobre-curvas-html`, razão `texto-vence-fallback=<bloco>`; unidade 249). A
  nova referência de identidade é o braço M1 congelado em `.frzero/wab_captura_bracos_24-09.json`
  (sha256 `04f2d59e…`; `.frzero/` é ignorado, só existe na máquina do usuário), conferido por
  `c1-3/wab_verifica_m1_src_24-09.py` → `identico: true`, 350/350 nos três eixos, 1 material com a
  razão nova. Na árvore integrada: rodar esse script e exigir o mesmo JSON; se
  `preservacao_base_v2_65_23-09` também for rodado, aceitar só aquela divergência. Qualquer outra
  divergência é erro de merge, não de regra. O tracker do motor já pede "baseline de preservação em
  clone limpo": com a #50 publicada, a captura vale ser refeita na própria `2589ed5a`, antes do merge.
- **§8.4**: os 5 testes novos do M1 e os 3 antigos que passaram a fixar `temporal_block_id` estão
  verdes na árvore mesclada (`test_core.py`, `test_reconcile_unit_block.py`,
  `test_resolver_apply_units.py`, `test_pedagogical_regeneration_order.py`: 418 passed).
- Nada disso mede acurácia; os números de unidade citados são os publicados pelo motor.

### 4.4 D12 (do usuário, no tracker) — pytest da CI

`python-quality.yml` roda em `ubuntu-latest`. As 26 falhas Linux da §6.1 (21 de caminho Windows /
`robocopy` / barra invertida, #68; `tkinter` e vazamento do mock, #69; 1 `google-genai`) reprovariam o
check `core` mesmo com o baseline do Ruff regerado. Confirma a dependência que o usuário registrou em
MOTOR-9-02; fora deste escopo propor a correção.

## 5. Gitleaks obrigatório na nuvem (tarefa 4)

| Teste (nesta VM, fora do repositório) | Resultado |
|---|---|
| `git show origin/main:scripts/security/gitleaks_scan.py > /tmp/gitleaks_scan.py; python3 /tmp/gitleaks_scan.py install --dest <tmp>` | **falha antes do argparse**: `IndexError: 2` em `REPO_ROOT = Path(__file__).resolve().parents[2]` (linha 39). O script pressupõe morar em `<raiz>/scripts/security/` |
| cópia em `<rascunho>/fora/scripts/security/gitleaks_scan.py`, `install --dest <tmp>` | **ok em 1,16 s**: download da release `linux_x64` pela rede do ambiente (proxy do agente, TLS íntegro), SHA-256 conferido, binário 8.30.1 (sha256 `88f91962…`, o mesmo que `~/.local/bin/gitleaks`) |
| segunda chamada | 0,37 s, mantém o binário (idempotente) |
| SessionStart da `main` (`setup_nuvem.sh`, `CLAUDE_CODE_REMOTE=true`) | rodou ao abrir a sessão (clone na `main`): "[nuvem] pre-commit instalado; gitleaks: 8.30.1"; `~/.local/bin` já estava no PATH (`CLAUDE_ENV_FILE` vazio) |
| hook mesclado (resolução do item 3) em repositório temporário | commit limpo passa; commit com token sintético bloqueado com "Remova do stage ou … exceção estreita" |

Consequências depois da integração: o hook obrigatório e o SessionStart passam a valer no motor e em
toda branch `nuvem/*`; sessão que nasce na `main` já os tem hoje. Sessão aberta antes da integração
numa branch de base motor não roda o SessionStart (o `settings.json` do motor não o tem) e, se o
script do ambiente ou o guia instalarem o `pre-commit.sh` do motor, ele volta a "só avisar". Se o hook
instalado for o da `main` e a branch não tiver `scripts/security/`, o commit exige `gitleaks` no PATH.

Propostas, sem editar nada nesta sessão:

1. **Script do ambiente** (fora do repositório; grava `/tmp/setup-nuvem.log`): depois do pip,
   instalar o gitleaks fixado independente da branch, respeitando a profundidade exigida:
   `mkdir -p /tmp/gl/scripts/security && git -C <clone> show origin/main:scripts/security/gitleaks_scan.py > /tmp/gl/scripts/security/gitleaks_scan.py && python3 /tmp/gl/scripts/security/gitleaks_scan.py install --dest "$HOME/.local/bin"`,
   registrando sucesso ou falha no log, e garantir `~/.local/bin` no PATH da sessão. Continuar sem
   instalar o hook ali (o SessionStart e a §1 do guia fazem isso na raiz do clone).
2. **Guia** (`.mex/context/sessao-nuvem.md` §1 e §2; `.mex/context/setup.md` bloco Linux): a primeira
   ação passa a incluir `gitleaks version` (esperado 8.30.1) e a leitura da saída do SessionStart;
   a linha "sem gitleaks ele só avisa" vira "com o hook da `main` (#83) o commit falha sem gitleaks;
   instalar com `python3 scripts/security/gitleaks_scan.py install --dest ~/.local/bin` (o script
   precisa estar em `scripts/security/` de alguma raiz)"; o `setup.md` do motor perde "O `gitleaks`
   também fica ausente; o hook só avisa" e ganha o comando. Isto é o D15.
3. **Opcional, issue própria na `main`** (código, fora deste escopo): `gitleaks_scan.py` calcular
   `REPO_ROOT` só quando `--config` não é dado, para o modo `install` funcionar de qualquer diretório.

## 6. Simulação da CI sobre a árvore mesclada atual (tarefa 5)

Cópia descartável de `e7779b38` no rascunho da sessão; nela, e só nela, os conflitos 1, 3, 10, 11 e
12 receberam a resolução proposta (§3), porque `pytest`, Ruff e os testes de guarda leem
`pyproject.toml`, `pre-commit.sh`, `.gitleaks.toml`, `settings.json` e importam `m365.py`; os 7
conflitos documentais ficaram com marcadores. Comando do `setup.md` (Linux), `TUTOR_COURSES_DIR`
inexistente, `-p no:cacheprovider --continue-on-collection-errors`; a árvore mesclada dispensa o
`--deselect` (fix da `main` em `test_core.py`) e rodou com `--cov=src`.

### 6.1 Comparação com o plano

| Verificação | Plano (24/09) | Agora (25/09) |
|---|---|---|
| Suíte na ponta do motor | 26 F / 2347 P / 30 S / 1 D / 2 E (`76e4357b`) | 26 F / 2354 P / 30 S / 1 D / 2 E (`2589ed5a`), 19,2 s |
| Suíte na árvore mesclada | 26 F / 2372 P / 30 S / 2 E (`5e7805a5`) | 26 F / 2419 P / 31 S / 2 E (`e7779b38`), 52,3 s com cobertura. +65 sobre o motor: os 25 do plano e 40 dos testes de cofre e guardas da #83; o skip novo é "DPAPI real só no Windows" |
| Conjunto de falhas | idêntico nos dois lados | idêntico (diff vazio); é a lista da §5.4 do plano: `test_moodle_structure` 5 + `test_moodle_sync` 13 + barra invertida 2 + `robocopy` 1 (#68), `test_subject_dialog_save` 3 + `test_reprocess_flags` 1 (#69), `google-genai` 1; erros de coleta `test_assessment_label_and_plan_section.py` e `test_codes_panel_label.py` (`tkinter`) |
| Ruff `py311` contra o baseline da `main` | 155 achados; 61 impressões digitais / 97 excedentes | **idêntico**: 155; 61 / 97 (`src` 18 fp / 32, `scripts` 6 / 15, `tests` 37 / 50; F401 59, B905 12, B023 11, B007 7, F841 4, B904 1, B008 1, F541 1, F811 1). Anexo A do plano continua válido; os arquivos novos dos dois lados não acrescentam achado |
| Contrato arquitetural | verde (1 = baseline) | verde |
| Cobertura (Linux, sem `tkinter`) | 84,19 % | 84,31 % (baseline Linux 79,62 %) |
| Check `core` | reprova (Ruff) | reprova (Ruff, D3) e reprovaria pelas falhas Linux (D12, §4.4) |
| Dirigidos da `main` (§8.2) | 40 passed | 40 passed |
| Check `secrets` (novo) | — | **reprova** nos passos A e C (§6.2); B e D verdes |

### 6.2 Check `secrets` (`.github/workflows/security.yml`) sobre a PR #9

| Passo | Comando equivalente rodado aqui | Resultado |
|---|---|---|
| A) versionado-ignorado | paths de `git ls-tree` de cada árvore contra o `.gitignore` respectivo (`check-ignore --no-index`) | `main`: 0. Motor `2589ed5a`: 8 (7 `.superpowers/brainstorm/**` + `tests/fixtures/cg/Cronograma2026-2.pdf`), como o tracker previu. **Árvore mesclada: 1**, só o PDF (a `main` apagou os brainstorms em #79 e o motor não os alterou, logo o merge os remove). O PDF foi adicionado com `-f` em `61cd4b04` como fixture do export SARC e é lido por `tests/test_pdf_markdown.py` e `tests/test_build_course.py` |
| B) caminho de credencial nos commits | `check_repo_hygiene.py range origin/main..origin/feat/motor-atribuicao` (1117 commits) | 0 violações, 2,8 s |
| C) gitleaks por commit | `gitleaks_scan.py range origin/main..origin/feat/motor-atribuicao` com o `.gitleaks.toml` da `main` (e também com o do motor: mesmo resultado) | **2 achados** `generic-api-key`, ambos falsos positivos: (i) `docs/reports/_codegraph-benchmark/manifest.json:97`, commit `e1133a43` (16/09): a chave é o caminho `src/builder/text/tokens.py` e o valor é o SHA-256 do arquivo (64 hex); a regra dispara pela palavra "token" no nome do arquivo; (ii) `docs/reports/_harness-2026-09-03/piloto-curvas/Curvas.htm:2384`, commit `e7ab84eb` (03/09): CSS `mso-*` do HTML exportado do Word, sem forma de credencial. Os dois arquivos seguem na ponta. 6,9 s. Para comparação, `MB..main` com a mesma regra dá 3: o token real em `40cfa28e` (a regra nova funciona) e 2 vezes `docs/reports/_context-mode-pilot/result.json:201`, chave `synthetic_secret_sha256` (sintético por desenho); a CI da `main` nunca os viu porque só varre commits novos |
| D) testes das proteções | `pytest tests/test_security_guards.py tests/test_credential_store.py tests/test_m365.py` com `REQUIRE_GITLEAKS=1` | 56 passed, 1 skipped (DPAPI), 19,5 s |

Exceções estreitas propostas para C (D14), acrescentadas ao `.gitleaks.toml` da `main` e validadas
nesta sessão (`range main..motor` → 0 achados; `MB..main` continua acusando o token de junho;
`tests/test_security_guards.py` 23 passed com a config alterada):

```toml
[[allowlists]]
description = "manifesto do benchmark do codegraph: chave = caminho de arquivo, valor = SHA-256 do arquivo"
targetRules = ["generic-api-key"]
condition = "AND"
paths = ['''^docs/reports/_codegraph-benchmark/manifest\.json$''']
regexTarget = "secret"
regexes = ['''^[0-9a-f]{64}$''']

[[allowlists]]
description = "HTML exportado do Word no piloto Curvas (CSS mso-*), sem credencial"
targetRules = ["generic-api-key"]
paths = ['''^docs/reports/_harness-2026-09-03/piloto-curvas/Curvas\.htm$''']
```

Sem elas, além da PR #9, o push do merge na `main` (`before..after` inclui os 1117 commits) também
reprovaria `secrets`.

## 7. Sequência do §7 do plano — só o que muda

- **Passo 1** (local): a continuação da #50 já está commitada (`15ea8c4a`); resta só commitar ou
  guardar as edições locais do tracker.
- **Passo 2** (higiene na `main`): **feito** por #79 e #83. Sai da sequência.
- **Passo 4** (resolver): 12 conflitos. `--theirs` (motor): `.gitignore` (mais a negação de D13),
  `AGENTS.md`, `.mex/ROUTER.md`. `--ours` (`main`): `.workflow/pendencias_workflow.md`,
  `.mex/patterns/engenharia-produto-details.md`, `README.md`, `.claude/settings.json`,
  `.gitleaks.toml` (mais D14). Manual: `pyproject.toml` (união), `docs/reports/pendencias.md`,
  `src/builder/sources/m365.py` (só `import os` no hunk), `scripts/hooks/pre-commit.sh` (hunk 1 motor,
  hunk 2 `main`). `git diff --check` depois.
- **Passo 4b (novo)**: coerência documental no mesmo commit (D15): `.mex/context/setup.md` linha 74 e
  `.mex/context/sessao-nuvem.md` §2 (linha do pre-commit/gitleaks) e §1.
- **Passo 5** (baseline do Ruff): inalterado (61 / 97 são os mesmos).
- **Passo 6** (validar): acrescentar, antes do commit do merge, `python scripts/security/check_repo_hygiene.py tree`
  (esperado 0), `python scripts/security/gitleaks_scan.py range origin/main..HEAD` (esperado 0) e o
  passo D com `REQUIRE_GITLEAKS=1`; replay conforme §4.3. Mensagem sugerida: "merge(main): integra
  #30/#39/#40/#62/#79/#83 em feat/motor-atribuicao; resolve 12 conflitos; regera baseline do Ruff".
- **Passo 7** (publicar): agora rodam `core` e `secrets` na PR #9; os dois precisam ficar verdes.
  Atualizar o corpo da PR (D8: #49, continuação da #50 publicada, grupos de revisão).
- **Passo 9** (reaplicar trabalho local): só o tracker.

## 8. Decisões do §9 — só o que muda

- **D4** fechada pelos fatos (§4.1); reescrita de histórico segue decisão separada.
- **D8** acrescentar à PR #9: `15ea8c4a` (continuação da #50) e a menção de "não commitada" sai.
- **D9** fechada: entrou antes da integração, na PR #9.
- **D12** (do usuário, tracker): confirmada em §4.4; segue aberta.
- **D13 (nova)** `tests/fixtures/cg/Cronograma2026-2.pdf`: manter a fixture e negar a regra
  (`!tests/fixtures/cg/Cronograma2026-2.pdf` no `.gitignore`, recomendado: 2 testes a leem e ela tem
  proveniência em `61cd4b04`) ou removê-la e adaptar os testes. Sem uma das duas, o passo A do check
  `secrets` reprova a PR #9.
- **D14 (nova)** exceções estreitas do gitleaks para os 2 falsos positivos (§6.2, validadas). Alternativa:
  apagar ou reescrever os dois arquivos de harness na ponta (não limpa o histórico; o check varre os
  commits, logo só a exceção resolve sem reescrita).
- **D15 (nova)** coerência de `setup.md` e `sessao-nuvem.md` sobre o hook obrigatório: no commit do merge
  (recomendado, é o commit que traz a obrigatoriedade ao motor) ou em PR própria.
- **D16 (nova, opcional)** issue na `main` para `gitleaks_scan.py install` funcionar fora de
  `scripts/security/` (§5, item 3).
- D1, D2, D3, D5, D6, D7, D10, D11: inalteradas.

## 9. Handoff (campos da §9 do guia da nuvem)

- **Pedido**: adendo da MOTOR-9-01 (Gate 1 dado pelo usuário em 25/09): atualizar o plano da PR #9
  para as SHAs atuais; só diagnóstico.
- **Issue/PR**: PR #9 (`Refs #9`); PR #77 (plano); PRs da `main` #79 e #83; issues #50, #80, #81, #82.
- **Base e SHA**: branch `nuvem/2026-09-24-motor-9-01-plano-integracao` @ `d5b98b9e` (sobre o motor
  `76e4357b`); diagnóstico feito sobre `main` `d815e205` e motor `2589ed5a`.
- **Branch**: `nuvem/2026-09-25-motor-9-01-adendo`.
- **Arquivos mudados**: só este relatório.
- **Testes** (Linux nuvem, sem `tkinter`, sem tutores, `TUTOR_COURSES_DIR` inexistente): motor
  `2589ed5a` 26 F / 2354 P / 30 S / 1 D / 2 E; árvore mesclada `e7779b38` (com as 5 resoluções de
  §6) 26 F / 2419 P / 31 S / 2 E, conjuntos de falha idênticos e todos preexistentes (§6.1);
  dirigidos 40 + 56 + 418 passed; Ruff 61 / 97 (= plano); arquitetura verde; cobertura 84,31 %;
  `check_repo_hygiene` range 0; gitleaks range 2 falsos positivos → 0 com D14; instalador do gitleaks
  ok fora do repositório na profundidade certa.
- **Não validado**: Windows, `tkinter`, replay (§4.3), zero-diff, CI real no GitHub, conteúdo local do
  worktree do usuário, `secrets` real (só simulado passo a passo).
- **Ambiente**: hook da `main` já instalado pelo SessionStart (clone nasce na `main`), gitleaks 8.30.1
  em `~/.local/bin`; commit deste relatório passou pelo hook no ramo "gitleaks direto".
- **Delta proposto ao tracker** (não aplicado; `pendencias.md` não editado): em MOTOR-9-01, acrescentar
  à evidência este adendo (branch `nuvem/2026-09-25-motor-9-01-adendo`, PR em rascunho sobre a #77) e
  "base 25/09: main `d815e205`, motor `2589ed5a`, 12 conflitos (9 + settings.json, gitleaks.toml,
  m365.py só imports); Ruff 61/97 inalterado; `secrets` reprovaria: PDF fixture versionado-ignorado
  (D13) e 2 falsos positivos do gitleaks no histórico (D14, exceções validadas)". Em MOTOR-9-02,
  dependências: acrescentar D13, D14, D15 e a recaptura do replay de preservação em `2589ed5a`
  (referência = braço M1, `wab_verifica_m1_src_24-09`); retirar "baseline exige clone sem a #50"
  (já commitada). Não criar a MOTOR-9-04 proposta no plano (higiene da `main`): entregue por #79/#83.
  MOTOR-9-03 e MOTOR-9-05 do plano inalteradas.
- **Decisões que ficam para o usuário**: D13–D16 (novas), D12 (sua), D1–D3, D5–D8, D10, D11.
