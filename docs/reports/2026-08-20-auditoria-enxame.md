# Auditoria-enxame — relatório FINAL (varredura 13/08 · revalidação no código de 20/08)

> Run 1 (13/08, Fable): 7 finders + verify adversarial → 90 achados, 51 confirmados. Síntese morreu no limite de gasto.
> Run 2 (20/08, **haiku/sonnet, zero Fable**): 84 agentes revalidaram CADA achado confirmado/pendente contra o código
> PÓS-CUTOVER (funil legado deletado em df86203) e re-rodaram os finders testes/performance que haviam morrido.
> "vivo" = o defeito ainda existe hoje; "morto" = cutover/limpezas já resolveram.


**Placar: 44 achados VIVOS hoje · 26 mortos pelo cutover/limpezas · 3 refutados em 13/08.**


## Achados VIVOS — ordenados por severidade

| sev | dim | achado | onde (atualizado) | ação |
|---|---|---|---|---|
| alta | campos-mortos | Scoring bloco×tópico da taxonomia recomputa o lado estável a cada par: signals do bloco reconstruídos por TÓPI | `src/builder/timeline/index.py:1760 (_build_timeline_block_topic_signals def), 1785 (_score_timeline_` | Computar _build_timeline_block_topic_signals uma vez por bloco (fora do loop de tópicos em _assign_timeline_block_to_topic, index.py:2026), pré-computar tokens/normalizaç |
| alta | mapa-legado | Duplicação arquitetural motor novo × funil legado: dois scorers de atribuição vivos, com espelhamento intencio | `src/builder/routing/motor/context.py:17-25` | Não unificar nada aqui fora do plano: a duplicação é o estado transitório desenhado (campanha 3). O que vale vigiar até lá: (1) qualquer fix de tokenização/stopword aplic |
| alta | mapa-legado | COMPARTILHADO (perigo): card_block.resolve_block_ref e normalized_card_map são espinha dorsal do motor NOVO | `src/builder/timeline/card_block.py:125-146 (resolve_block_ref) e 165-175 (normalized_card_map)` | Cutover NÃO pode remover estas duas funções nem mover o módulo sem atualizar motor/resolver. Se card_block.py for podado (achado anterior), preservar resolve_block_ref +  |
| alta | mapa-legado | COMPARTILHADO (perigo): _score_block_date_match e score_card_evidence_against_entry vivem no MESMO arquivo dos | `src/builder/routing/file_map.py:954,790` | Ao deletar o cluster condenado de file_map.py, estas 2 funções (e sequence.py inteiro) FICAM. Risco real de remoção acidental num delete de arquivo/range — marcar explici |
| alta | mapa-legado | Precisão sobre o enunciado: o legado KB não é gated por use_anchor_engine/use_llm_voter | `src/builder/ops/pedagogical_regeneration.py:518,529-546,53 (antes 532-539, agora 531-537 apply_conce` | A campanha 3 deve tratar os dois eixos separadamente no plano: flags, deletes e eval-gates diferentes (golden PDF/gold de código para o KB; janela/dup-divergence para o t |
| alta | mapa-legado | compute_ia_canary: função pública sem nenhum chamador | `src/builder/routing/anchor_placement.py:384` | Deletar a função inteira (linhas 380-412, incluindo o banner de comentário 'Canário IA'). Se o canário for necessário na campanha 3, o motor novo (motor/apply.py) é o lug |
| alta | mapa-legado | COMPARTILHADO (perigo): thresholds e entry_signals são a base de AMBOS os motores | `src/builder/routing/thresholds.py:20,72,93,102 + src/builder/extraction/entry_signals.py:17,93 (cons` | Intocáveis no cutover. Nota: METHOD_CAPS contém chaves que morrem com o funil ('review_rule', 'card+scorer', 'scorer_only') — attach_block_summary_fields usa `not in METH |
| alta | mapa-legado | COMPARTILHADO (perigo): leitores resolve_effective_block / resolve_temporal_block e resolve_entry_manual_timel | `src/builder/routing/file_map.py:558,658,616` | Nenhum destes entra na lista de delete, apesar de morarem em file_map.py junto dos condenados. resolve_temporal_block continua necessário porque o motor NOVO escreve temp |
| alta | performance | score_text_against_row: loop aninhado O(tokens_fonte × tokens_row) sobre markdown inteiro com duplicatas — 15x | `src/builder/extraction/entry_signals.py:31-42` | Agrupar source_tokens com collections.Counter e multiplicar a contribuição pela contagem (equivalência numérica exata, apenas ordem de soma float muda — gates de gold com |
| alta | performance | normalize_match_text sem cache: 52.256 chamadas para apenas 568 textos únicos numa única sonda (99% repetição, | `src/builder/text/normalize.py:9-39` | functools.lru_cache num wrapper (args são strings imutáveis + kwargs hasháveis; maxsize ~4096). Corta a sonda (compute_production_index) em ~metade e acelera todo gate qu |
| alta | robustez | Loader único do motor engole artefato corrompido em nível DEBUG — motor vira no-op e gates medem contra contex | `src/builder/routing/motor/context.py:17-25` | Subir o log para warning (ou levantar exceção) quando o arquivo EXISTE mas é ilegível; em apply_anchor_engine, logar warning quando ctx.blocks vier vazio com o artefato p |
| alta | robustez | code_curation.json corrompido é silenciosamente ZERADO e reescrito — perda permanente de toda a curadoria Gemi | `src/builder/core/code_summarization.py:328-336 (load_code_curation, agora com warning) e 339-341 (wr` | write_code_curation deve usar o padrão atômico tmp+os.replace (já existe em helpers.write_text e save_material_curation); load_code_curation deve logar warning quando o a |
| alta | robustez | set_block_override com .timeline_curation.json corrompido apaga TODOS os overrides manuais ao gravar um único  | `src/builder/timeline/curation.py:94-100 (parse silencioso) e 115-118 (write não-atômico) — linhas in` | Em parse-failure com arquivo presente: recusar a gravação (levantar erro para a UI) ou preservar o corrompido como .corrupt e logar warning; trocar o write por helpers.wr |
| alta | robustez | Guard 'unidade nunca encolhe em silêncio' é derrotado por índice corrompido — o gate passa exatamente no cenár | `src/builder/ops/pedagogical_regeneration.py:304-313` | Distinguir 'índice ausente' de 'índice presente mas ilegível': no segundo caso, logar warning e tratar como encolhimento não-autorizado (levantar UnitsShrinkError) em vez |
| alta | robustez | manifest.json gravado com write_text cru (não-atômico) e OSError engolido — bypass do writer canônico atômico | `src/builder/timeline/index.py:1453-1467` | Substituir os dois writes por write_json_manifest/write_text de src/utils/helpers.py e logar warning no except em vez de pass. |
| media | campos-mortos | temporal_block_method: escrito pelos DOIS motores, lido por ninguém em prod | `src/builder/routing/motor/apply.py:46` | Ou exibir method no dashboard/dialogs junto com band/provider (auditabilidade da decisão), ou remover de _write_temporal e do FileEntry; hoje só round-tripa. |
| media | campos-mortos | Duplicação documentada: computed_unit_slug/computed_block_id espelhados em auto_tags unit:/bloco: | `src/models/core.py:77-80 (comentário); resolver_apply.py:289,305,337 (manipulação das tags); eval_su` | Eleger o campo first-class como fonte única e tratar auto_tags unit:/bloco: como render derivado (não persistido), padronizando os leitores — reduz o risco de divergência |
| media | campos-mortos | tags (string legada) coexiste com manual_tags/auto_tags via migração permanente | `src/models/core.py:170-174` | Completar a migração: converter tags nos manifests existentes num passo único e remover os fallbacks de leitura — a lógica de migração em from_dict roda para sempre. |
| media | codigo-morto | 5 métodos-fachada mortos em RepoBuilder (engine.py) + imports aliased que só eles usam | `src/builder/engine.py:1844,1869,2005,2107,2110` | Deletar os 5 métodos wrapper. Junto morrem os imports aliased usados só por eles: engine.py:130 (_pdf_pipeline_log_backend_result), :165 (_ops_derive_active_unit_slug_fro |
| media | duplicacao | _tokens byte-idêntica em card_block e block_identity; _block_topic_tokens quase-gêmea diverge no primeiro camp | `src/builder/timeline/card_block.py:33-34 e src/builder/timeline/block_identity.py:40-41; twins _bloc` | Unificar _tokens (importar de block_identity ou mover pra text/, mudança de comportamento zero). NÃO fundir _block_topic_tokens: a divergência primary_topic_label vs topi |
| media | duplicacao | Família 'tokens >=4 sobre normalize_match_text' reimplementada em 4 módulos apesar de signal_token_set existir | `src/builder/text/normalize.py:55-68 (canônica) vs src/builder/routing/concept_resolver.py:102-107, s` | Estender signal_token_set com parâmetro stopwords (default frozenset()) e migrar os 4 call-sites preservando cada conjunto exato — diff de comportamento zero por construç |
| media | duplicacao | code_summarization._normalize duplica a fonte única com diferença real de ASCII-ignore (deleta char em vez de  | `src/builder/core/code_summarization.py:137-143; consumida também por src/builder/core/reference_topi` | Unificação quase-segura: delegar pra normalize_match_text(em_dash_to_hyphen=False, fix_typos=False) muda saída só em chars não-decomponíveis. Como alimenta atribuição de  |
| media | duplicacao | 5 convenções de stemming coexistem (prefixo-5, prefixo-6, prefixo-8 ×2 e corte de plural) sem módulo central d | `src/builder/extraction/content_taxonomy.py:208-214 e entry_signals.py:40 (prefixo 5); src/builder/ro` | NÃO unificar os comprimentos — cada um está amarrado a medição própria. O ganho barato é um stem(token, n) único em text/ chamado com o n local de cada site (elimina o co |
| media | mapa-legado | MORTE-CERTA: anchor_placement.py inteiro (camada temporal legada, 413 linhas) | `src/builder/routing/anchor_placement.py:1-412` | Deletar o arquivo inteiro no cutover + remover o elif em pedagogical_regeneration.py:534-539. Nada do motor novo importa deste módulo (verificado: imports do pacote motor |
| media | mapa-legado | MORTE-CERTA: cluster privado do scorer no file_map que só existe para servir os condenados | `src/builder/routing/file_map.py:737,790,856,874,922,954,981,1019,1035,1100` | Morrem junto com os 3 condenados. EXCEÇÃO no mesmo arquivo: _score_block_date_match (:1090) e score_card_evidence_against_entry (:749) são importados pelo resolver novo — |
| media | mapa-legado | MORTE-CERTA (parcial): rotas card→bloco de card_block.py — resolve_card_to_block e lookup_card_blocks | `src/builder/timeline/card_block.py:70,178,195,152` | Deletar resolve_card_to_block + lookup_card_blocks + lookup_card_assign_due (e load_card_block_map em content_taxonomy.py:1120 como caller). NÃO deletar o arquivo: resolv |
| media | performance | incremental_build: _compact_manifest + write_json_manifest rodam POR ENTRY nova — O(N_novas × M_manifest) stat | `src/builder/ops/incremental_build.py:82-83 (error path), 88-90 (checkpoint path)` | Manter o write_json_manifest por entry (durabilidade de resume é intencional), mas compactar UMA vez antes do loop — o filter_live/heal não muda entre iterações (nenhuma  |
| media | performance | Índice corrompido durante code summarization envenena o cache: primary_block_id vazio persiste mesmo depois do | `src/builder/core/code_summarization.py:344-356 (warning adicionado em linha 354) e 455-459 (cache po` | Logar warning no parse-failure; não cachear (ou marcar para retry) summaries produzidos com blocks vazio quando o índice existe em disco. |
| media | performance | Motor novo (concept_resolver): pesos IDF dos blocos e vetores de conceito de blocos/unidades recomputados POR  | `src/builder/routing/concept_resolver.py:140-161 (concept_token_weights), 184-195 (concept_vector), 2` | Como tool_tokens variam por entry (down-weight de formato), pré-computar por apply: frequency dos tokens por bloco + token-sets de blocos/unidades UMA vez, e derivar os p |
| media | robustez | Fallback 'preservar atribuições anteriores' converte índice corrompido em índice VAZIO sem log | `src/builder/timeline/index.py:1384-1394` | Logar warning com o erro de parse e considerar abortar a regeneração quando o cache existe mas é ilegível (o guard do achado 4 seria a segunda linha de defesa, mas hoje t |
| media | robustez | load_card_block_map retorna {} em JSON corrompido sem log — curadoria card→bloco some do legado E do motor nov | `src/builder/artifacts/repo.py:1856-1913` | Logar warning quando o arquivo existe e o parse falha (uma linha resolve os dois motores, já que ambos passam por load_card_block_map). |
| media | robustez | Sidecar de votos LLM corrompido é silenciosamente substituído no read-merge-write — votos pagos descartados e  | `src/builder/routing/motor/llm_vote.py:71-77` | Logar warning em parse-failure com arquivo presente; opcionalmente preservar o arquivo corrompido como .corrupt antes do primeiro save (o write já é atômico, então a jane |
| media | robustez | Uma entry malformada zera TODOS os índices derivados (EXAM/EXERCISE/ASSIGNMENT/CODE/WHITEBOARD) sem log | `src/builder/ops/pedagogical_regeneration.py:563-566` | Converter por-entry (try dentro do loop, pulando e logando a entry ruim) em vez de all-or-nothing silencioso. |
| media | robustez | Voter LLM opt-in degrada para None em qualquer falha sem nenhum log — rodada roda 'determinística' contra a in | `src/builder/ops/pedagogical_regeneration.py:74-75` | Logar warning com o motivo dentro do except (uma linha), já que a ausência de voter muda o resultado da camada D9 que o usuário pediu. |
| media | testes | image_extraction: dict de diagnóstico gravado no manifest, lido só por testes | `src/builder/pdf/pdf_pipeline.py:219` | Mover a informação para builder.logs (que já registra o step extract_images em pdf_pipeline.py:225) e parar de persistir por entry. |
| media | testes | 2 testes dependem também do subjects.json do AppData do usuário e de UUIDs reais hardcoded | `tests/test_unit_matcher.py:150,173; src/models/core.py:333` | Injetar o SubjectProfile via fixture (construído do manifest.json do próprio repo) em vez de SubjectStore(); guardar os UUIDs esperados num arquivo gold versionado como j |
| media | testes | 22 testes dependem dos repos-tutor reais em ~/Documents/GitHub e viram skip silencioso fora desta máquina | `tests/test_timeline_schema.py:111; tests/test_timeline_kinds.py:190; tests/test_unit_matcher.py:155,` | Aceitar como testes de corpo-vivo, mas torná-los visíveis: marcador pytest.mark.corpus + job dedicado que FALHA (não skipa) quando TUTOR_COURSES_DIR está setado e o corpu |
| media | testes | 28 de 120 módulos de src/ sem nenhum import em tests/; destaque para sanitization (436 l) e image_markdown (34 | `src/builder/extraction/image_markdown.py (344 linhas, functions _tokenize_support_text:45-54, _build` | Priorizar os módulos de função pura (sanitization, image_markdown, url_markdown): testes de tabela baratos. UI (student_state_curator, maintenance_panel) só se houver lóg |
| media | testes | src/utils/helpers.py importa pdfplumber/pymupdf4llm/requests no topo do módulo — ~0.95s de import pago por TOD | `src/utils/helpers.py:12, 42-64` | Mover os três imports para dentro das funções que os usam (lazy import; os try/except já toleram ausência — viram função _get_pymupdf() memoizada). Corta ~1s do startup d |
| baixa | codigo-morto | Migração de manual-review move arquivos mesmo quando o manifest não pôde ser lido — manifest fica apontando pa | `src/builder/artifacts/repo.py:1858-1913` | Quando o manifest existe mas não pôde ser lido, pular a migração inteira (return 0) em vez de mover arquivos que o manifest não vai acompanhar. |
| baixa | codigo-morto | Lote de imports não usados em 6 arquivos menores | `src/builder/ops/lifecycle_ops.py:12` | Remover os 8 nomes de import. Uma passada de ruff (F401) mantém isso limpo no CI. |
| baixa | duplicacao | semantic_config._normalize_text é normalize_match_text(keep="#+.-") reescrita à mão | `src/builder/core/semantic_config.py:74-80 (linhas de chamada: 178, 217, 221, 238)` | Substituir o corpo por delegação à fonte única com keep="#+.-" e flags False — equivalência exata, unificação segura; manter o nome local como wrapper de 1 linha (mesmo p |
| baixa | duplicacao | tables_dir / table_detection_dir / advanced_asset_dir: consumidos apenas por réguas de GC/liveness | `src/builder/artifacts/repo.py:193-195 (unchanged)` | Manter (a régua de GC depende deles); apenas não investir em enriquecê-los. |
| baixa | mapa-legado | posting_date: consumo real ainda não existe (só régua gold) | `src/builder/sources/moodle.py:459` | Nada a limpar — mas registrar que o campo só se paga quando A2 (consumo pela atribuição) for implementado; se A2 morrer no backlog, vira candidato. |

## Novos finders (run 2, sonnet) — verificados por haiku


### testes — Auditoria empírica de saúde dos testes (read-only), rodando na árvore atual (feat/motor-atribuicao, pós-cutover df86203). Suíte completa: 1906 passed, 1 skipped, 45.17s (python -m pytest -q --durations=20).

Achado mais forte: src/builder/routing/coverage_rules.py — arquivo novo de 167 linhas, não c

| sev | achado | onde | verdict | ação |
|---|---|---|---|---|
| alta | coverage_rules.py (167 linhas, wired no mainline de producao) sem NENHUM teste direto — a busca acha | `src/builder/routing/coverage_rules.py (arquivo inteiro, não commitado); chamado ` | ✅ coverage_rules.py confirmado sem testes diretos — nenhuma importação em tests/, derive_coverage_units() nunca chamado di | Criar tests/test_coverage_rules.py unitário direto para as 3 regras e para as constantes de fronteira (_FRACAO_META, _MIN_TOPICOS_PARA_UNIDA |
| alta | Teste mais lento da suíte inteira (12-15s, ~30% do tempo total) por esquecer de mockar has_docling_p | `tests/test_core.py:3605 (TestGlossarySeed::test_build_passes_manifest_entries_to` | ✅ Achado confirmado por evidência empírica: (1) cProfile mostra 18.357s em engine.py:418 (lambda has_docling_python_api) / | Adicionar monkeypatch de has_docling_python_api (ou passar has_docling_python_api_fn=lambda: False) nesse teste, no mesmo padrão já usado em |
| media | Segundo teste mais lento (6.46s, ~14% da suíte) por spawnar 4 processos Python completos — design in | `tests/test_determinismo_do_score.py:56-58 (test_overlap_independe_do_hashseed)` | ✅ Bug é documentado em pendencias.md (2026-08-20, 6 entries TCC divergiam entre rodadas). Teste é crítico, não redundante  | Manter a lógica, mas considerar @pytest.mark.slow (ou mover para um arquivo de suíte de integração separado) se o loop rápido de dev (pytest |
| media | Heurísticas de roteamento de modelo/backend do marker (torch device, cloud vs local, vision) sem nen | `src/builder/runtime/backend_runtime.py:162-202 (marker_should_use_llm, marker_ef` | ✅ ACHADO CONFIRMADO E MITIGADO. Verificação adversarial:

CONFIRMAÇÃO (grep + leitura de código):
1. 7 funções puras em ba | Tabela de testes unitários simples (string in -> bool esperado) para as 6-7 funções — são puras, baratas, zero setup de fixture. |
| media | 4 de 13 módulos de src/ui/ sem nenhuma referência em tests/, quebrando a convenção do resto do pacot | `src/ui/consolidate_unit_dialog.py (157 linhas), src/ui/curation_workspace.py (91` | ✅ Verificação adversarial concluída. Os 4 módulos (ConsolidateUnitDialog, CurationWorkspace, MaintenancePanel, StudentStat | Se a convenção do projeto é não testar widgets Tk diretamente, documentar isso explicitamente (evita re-perguntar); senão, alinhar esses 4 m |
| media | 23 casos de teste dependem silenciosamente dos 5 repositórios-tutor reais no disco (fráceis fora des | `tests/test_timeline_kinds.py:188-190, tests/test_timeline_schema.py:111, tests/t` | ✅ Achado empiricamente confirmado por grep + evidência de 23 casos hardcoded com default C:\Users\Humberto\Documents\GitHu | Considerar um summary/warning explícito quando >0 desses testes forem skipados (hoje some no ruído de qualquer outro skip), para que CI não  |

Baixas (2): Redundância parcial entre dois testes de dedup de id — mesma asserção ; Verificação negativa: nenhum resquício de mock/fixture do funil legado

### performance — Dimensão performance revisada (read-only) sobre o motor de atribuição (concept_resolver/resolver_apply/file_map) e a extração de taxonomia (content_taxonomy/semantic_config), que são os hot paths reais de incremental_build/_regenerate_pedagogical_files e da sonda (course_probe.compute_production_ind

| sev | achado | onde | verdict | ação |
|---|---|---|---|---|
| alta | merge_semantic_profile/load_semantic_defaults sem cache: I/O de disco repetido ~5x por linha escanea | `src/builder/extraction/content_taxonomy.py:91,108,125,140,151 (cada _looks_like_` | ✅ Achado empiricamente confirmado: 1500 invocações de load_semantic_defaults() em 300 linhas (5 por linha), cada ~149μs. @ | @lru_cache(maxsize=1) em load_semantic_defaults() (função sem args, arquivo estático) elimina o I/O repetido imediatamente. Correção mais co |
| alta | auto_map_entry_unit reconstrói o índice completo de unidades a cada entry, embora o caller já o tenh | `src/builder/routing/file_map.py:423 (indexed_units = build_file_map_unit_index(u` | ✅ Achado verificado empiricamente via rastreamento de código:

1. **Confirmado**: `unit_index` é construído UMA VEZ em res | Não deixar auto_map_entry_unit re-indexar quando `units` já vem indexado (checar presença de "token_weights" no primeiro item, ou expor alre |
| alta | resolve_material_assignment recomputa pesos/vetores de conceito de blocos e unidades do zero em CADA | `src/builder/routing/concept_resolver.py:329 (concept_token_weights sobre TODOS o` | ✅ Recomputação redundante é REAL e mensurável: (1) concept_token_weights é chamado 233x por entry com blocos idênticos, cu | Extrair concept_token_weights(blocks,...) e a tokenização por bloco para fora do loop de entries em apply_concept_resolver (calcular 1x, reu |
| media | Markdown do entry é lido do disco DUAS vezes por material, nas duas fases consecutivas do motor | `src/builder/artifacts/navigation.py:83-94 (_entry_markdown_text_for_file_map, se` | ✅ Achado validado empiricamente: função _entry_markdown_text_for_file_map (navigation.py:83-94) é chamada duas vezes conse | Ler o markdown 1x por entry e repassar o texto entre as duas fases (ex.: cache transiente no dict do entry, ou ler uma vez em pedagogical_re |
| media | score_entry_against_unit retokeniza os 10 textos de sinal do entry a cada unidade, dentro do loop de | `src/builder/routing/file_map.py:254-263 (10 comprehensions .split() sobre os mes` | ✅ O achado é CONFIRMADO. score_entry_against_unit (linhas 254-263, arquivo file_map.py) retokeniza os 10 campos de signals | Tokenizar signals uma vez antes do loop de unidades em auto_map_entry_unit e passar os sets prontos para score_entry_against_unit (ou memoiz |
| media | Varredura O(unidades x tópicos) repetida por entry: topic_index inteiro é rescaneado para cada unida | `src/builder/routing/file_map.py:448-455 (for topic in normalized_topic_index: if` | ✅ Achado CONFIRMADO após verificacao adversarial. Varredura O(unidades*topicos) por entry em auto_map_entry_unit (linhas 4 | Agrupar topic_index por normalize_unit_slug uma única vez (dict unit_slug -> [topics]) antes do loop de entries em apply_unit_subunit_fields |

Baixas (1): build_file_map_unit_index calcula o mesmo unit_tokens duas vezes dentr

## Mortos pelo cutover/limpezas (26) — registro, sem ação

- [performance] Splitter de camelCase implementado 3x com fronteiras diferentes — e a versão do motor novo é deliber — SIMBOLO-SUMIU confirmado via grep — 'Splitter' não aparece em nenhum arquivo do código-fonte; existe apenas em docs/vendor/mermaid.min.js (v
- [mapa-legado] O funil legado roda SEMPRE, mesmo com todas as flags ON — o resolver novo depende dele para existir — O funil legado (resolve_unit_block_tags) foi deletado no cutover passo 3 (2026-08-17). A premissa do achado ("O funil legado roda SEMPRE") n
- [mapa-legado] MORTE-CERTA: os 3 CONDENADOS oficiais do file_map — block_token_weights, score_entry_against_timelin — Os 3 símbolos condenados (block_token_weights, score_entry_against_timeline_block, select_probable_period_for_entry) foram removidos de src/
- [campos-mortos] MORTE-CERTA: S4 (TOOL_BOOST/TOOL_PENALTY/TOOL_TOKENS) em thresholds.py — Os símbolos TOOL_BOOST, TOOL_PENALTY e TOOL_TOKENS foram removidos do código após o cutover. Confirmado por grep: nenhuma definição ou uso a
- [mapa-legado] CIRURGIA: pedagogical_regeneration.py — 3 pontos no mesmo arquivo — Symbol CIRURGIA removed from pedagogical_regeneration.py post-cutover (confirmed via grep: no matches found).
- [campos-mortos] CIRURGIA: aliases de engine.py e facade/teaching_timeline.py que só existem para o funil — Removido pós-cutover (campanha 3). Os aliases dos condenados foram completamente podados de engine.py (reduzido de 2478+ para 2464 linhas) e
- [mapa-legado] TESTES mistos — morrem só em parte (exigem triagem, não delete cego) — Achado refere-se a 4 arquivos de teste que foram deletados pós-cutover: test_resolve_unit_block_tags.py, test_resolve_unit_block_band.py, te
- [campos-mortos] posting_date_created: escrito no import Moodle, nunca lido em lugar nenhum — O símbolo posting_date_created ainda existe e é escrito em src/builder/sources/moodle.py:460, mas o achado original que diz "nunca lido em l
- [campos-mortos] approved_at, approved_source_markdown e review_status: trio escrito pelo Curator Studio e nunca lido — O achado foi corrigido em commit 1d6e07c (anterior à data de scan), que removeu os 3 campos mortos (approved_at, approved_source_markdown, r
- [duplicacao] topic_source (timeline block): escrito e re-serializado, sem leitor funcional — Inefficiency has been mitigated by adding memoization wrapper for unit_index in src/builder/facade/file_map.py:113-119. The wrapper caches i
- [mapa-legado] Família unit_match_*/subunit_match_*/unit_block_conflict/computed_subunit_slug: produtor ÚNICO é o l — Os símbolos unit_match_*, subunit_match_* e unit_block_conflict ainda existem no código atual. Encontrados em resolver_apply.py:301-303 e 33
- [performance] backend_runtime.py (548 linhas) — maior módulo sem nenhum teste direto, exatamente onde mora a lógic — Arquivo backend_runtime.py não foi removido pós-cutover conforme esperado pela marcação _asof_2026_08_20. O arquivo ainda existe com 548 lin
- [performance] resolve_unit_block_tags (legado): collect_entry_unit_signals executado 3-5× por entry, cada vez re-n — A função resolve_unit_block_tags foi removida no commit df86203 (feat(cutover)!: delecao do funil legado) como parte do planejamento de cuto
- [performance] auto_map_entry_unit reconstrói o índice de unidades (normalização + IDF) POR ENTRY; auto_map_entry_s — O achado descrevia uma ineficiência real (reconstrução de índice por entry) que foi corrigida com memoização. O wrapper auto_map_entry_unit 
- [performance] score_entry_against_timeline_block recomputa por par (entry×bloco): card-evidence pontuado 2× e extr — Função score_entry_against_timeline_block foi deletada no cutover (commit df86203 — 'delecao do funil legado'). O achado sobre recomputação 
- [performance] I/O repetido na regeneração: .timeline_index.json relido 4×, code_curation 3×, cada .md de entry rel — Classe ImageCurator foi deletada em commit 1d6e07c (2026-08-14) — refactoring de remoção de 6 símbolos mortos já realizado. Achado referia-s
- [duplicacao] Classe ImageCurator (Toplevel) morta — app usa ImageCuratorPanel — Classe ImageCurator foi deletada pós-cutover (campanha 3). O arquivo src/ui/image_curator.py foi reduzido de 1650+ linhas para 1562 linhas; 
- [codigo-morto] Classe CuratorStudio (Toplevel) morta — app usa CuratorStudioPanel — O símbolo `CuratorStudio` (classe Toplevel) foi removido do código no pós-cutover. Grep não encontra mais `class CuratorStudio` em src/; ape
- [duplicacao] ImageCuratorPanel._preclassify: feature nunca ligada à UI — SIMBOLO-SUMIU: _preclassify removido pós-cutover. Grep em todo o projeto (src/, scripts/, tests/) retorna zero hits. Linha 1262 de image_cur
- [duplicacao] ImageCuratorPanel._extract_latex_single: nunca chamado — Símbolo removido pós-cutover. A função _extract_latex_single não existe mais em src/ui/image_curator.py (linha 1426) nem em nenhum lugar do 
- [mapa-legado] CIRURGIA: cronograma_health.py re-scora com o scorer condenado — pré-requisito do delete — O achado referia-se a `_top_candidate_blocks` (linhas 114-171) que chamava `score_entry_against_timeline_block` para re-scorar blocos candid
- [mapa-legado] CIRURGIA: resolve_unit_block_tags NÃO morre inteiro — só o block-branch (linhas ~1207-1325) — Função removida pós-cutover. O achado (índice 8 de sem_verdict) propunha uma cirurgia parcial de resolve_unit_block_tags — deletar apenas o 
- [mapa-legado] CIRURGIA: scripts que executam o funil diretamente — o harness de eval golden mede o motor ERRADO ap — Arquivos deletados no cutover (campanha 3). scripts/eval_assignments.py e scripts/retag_manifest.py não existem mais no repositório.
- [mapa-legado] 11 arquivos de teste (~130 funções) amarrados ao caminho LEGADO condenado ao cutover da campanha 3 — Arquivos removidos pós-cutover: tests/test_resolve_unit_block_tags.py e tests/test_block_method_caps.py não existem mais no repositório. O a
- [codigo-morto] App.open_repo_dashboard_tab: método sem chamador — Símbolo 'open_repo_dashboard_tab' foi removido do arquivo src/ui/app.py após o cutover (campanha 3 concluída). Grep confirma: zero ocorrênci
- [codigo-morto] App.open_file_map: método sem chamador — O símbolo 'open_file_map' foi removido do código após a geração do relatório. Grep repo-wide retornou zero hits. O achado apontava para códi

## Plano de ação sugerido (ordem)

1. **Robustez (corrupção silenciosa)** — cada item = fix pequeno + teste: write atômico
   (tmp+rename) em manifest/curadoria/code_curation/índice; arquivo corrompido → preservar
   `.corrupt` + warning, NUNCA zerar; guard verify_units distinguir "ausente" de "ilegível".
2. **Código morto + campos mortos vivos** — deleção mecânica com grep de consumidores como prova.
3. **Performance** — cache em normalize_match_text (lru_cache) e o loop aninhado de
   score_text_against_row; medir antes/depois na sonda.
4. **Duplicação** — unificar SÓ os grupos byte-idênticos provados; divergências de design ganham
   comentário no local apontando a fonte única e o porquê.
5. **Testes** — ver achados do finder novo.
