# Auditoria-enxame — relatório reconstruído (varredura as-of 2026-08-13, cruzada com código as-of 2026-08-20)

> Workflow de 7 finders + verificação adversarial + síntese. A síntese e parte dos verifiers morreram
> no **limite mensal de gasto**; este relatório foi reconstruído do journal (61 agentes, ~4M tokens)
> pelo controlador, sem gastar agentes. ATENÇÃO: a varredura viu o código PRÉ-cutover (13/08); a
> coluna "as-of 20/08" é um cruzamento BARATO (arquivo/símbolo ainda existe?) — não re-verificação.
> Dimensão é inferida por vocabulário (o journal não guarda labels).
>
> Totais: **90 achados** → **51 confirmados** · 3 refutados · 19 sérios sem verdict · 25 baixas.
> Dos 51 confirmados: 37 com símbolo vivo hoje, 9 já sumiram (cutover/limpezas de 14–19/08), 5 inconclusivos.


## Confirmados pela verificação adversarial (51)

| sev | dim | achado | onde | as-of 20/08 | ação |
|---|---|---|---|---|---|
| alta | campos-mortos | posting_date_created: escrito no import Moodle, nunca lido em lugar nenhum | `src/builder/sources/moodle.py:460` | ✅ vivo | Remover do FileEntry e do import Moodle (ou documentar como campo de diagnóstico externo); item direto do backlog de limpeza. |
| alta | campos-mortos | approved_at, approved_source_markdown e review_status: trio escrito pelo Curator Studio e nunca lido | `src/ui/curator_studio.py:936-938` | ✅ vivo | Ou remover os 3 campos do approve flow, ou passar a exibi-los na UI (ex.: badge 'aprovado em' no dialog); hoje são peso morto gravado em todo manifest. |
| alta | campos-mortos | Scoring bloco×tópico da taxonomia recomputa o lado estável a cada par: signals do bloco reconstruídos por TÓPI | `src/builder/timeline/index.py:1959-1961, 1866-1872, 1874-1906, 121-128` | ✖ sumiu (simbolo-sumiu) | Computar _build_timeline_block_topic_signals uma vez por bloco (fora do loop de tópicos em _assign_timeline_block_to_topic, index.py:2026), pré-computar tokens/ |
| alta | codigo-morto | Classe CuratorStudio (Toplevel) morta — app usa CuratorStudioPanel | `src/ui/curator_studio.py:1464` | ✅ vivo | Deletar a classe CuratorStudio (curator_studio.py:1464-1479). |
| alta | duplicacao | Classe ImageCurator (Toplevel) morta — app usa ImageCuratorPanel | `src/ui/image_curator.py:1650` | ✅ vivo | Deletar a classe ImageCurator (image_curator.py:1650-1665). |
| alta | mapa-legado | Duplicação arquitetural motor novo × funil legado: dois scorers de atribuição vivos, com espelhamento intencio | `src/builder/routing/motor/disambiguator.py:1-42 (espelha marco0; PROIBIDO importar legado)` | ◐ inconclusivo | Não unificar nada aqui fora do plano: a duplicação é o estado transitório desenhado (campanha 3). O que vale vigiar até lá: (1) qualquer fix de tokenização/stop |
| alta | mapa-legado | O funil legado roda SEMPRE, mesmo com todas as flags ON — o resolver novo depende dele para existir | `src/builder/ops/pedagogical_regeneration.py:507 + src/builder/routing/resolver_apply.py:11` | ✖ sumiu (simbolo-sumiu) | O cutover NÃO é 'deletar o funil e ligar a flag'. Antes de deletar o block-branch do funil é preciso remover o skip da linha 116 (ou fazer o resolver atribuir d |
| alta | mapa-legado | COMPARTILHADO (perigo): card_block.resolve_block_ref e normalized_card_map são espinha dorsal do motor NOVO | `src/builder/timeline/card_block.py:122,159` | ✅ vivo | Cutover NÃO pode remover estas duas funções nem mover o módulo sem atualizar motor/resolver. Se card_block.py for podado (achado anterior), preservar resolve_bl |
| alta | mapa-legado | COMPARTILHADO (perigo): _score_block_date_match e score_card_evidence_against_entry vivem no MESMO arquivo dos | `src/builder/routing/file_map.py:1090,749` | ✅ vivo | Ao deletar o cluster condenado de file_map.py, estas 2 funções (e sequence.py inteiro) FICAM. Risco real de remoção acidental num delete de arquivo/range — marc |
| alta | mapa-legado | Precisão sobre o enunciado: o legado KB não é gated por use_anchor_engine/use_llm_voter | `src/builder/ops/pedagogical_regeneration.py:518,532-539,53` | ✅ vivo | A campanha 3 deve tratar os dois eixos separadamente no plano: flags, deletes e eval-gates diferentes (golden PDF/gold de código para o KB; janela/dup-divergenc |
| alta | mapa-legado | compute_ia_canary: função pública sem nenhum chamador | `src/builder/routing/anchor_placement.py:384` | ✅ vivo | Deletar a função inteira (linhas 380-412, incluindo o banner de comentário 'Canário IA'). Se o canário for necessário na campanha 3, o motor novo (motor/apply.p |
| alta | performance | Splitter de camelCase implementado 3x com fronteiras diferentes — e a versão do motor novo é deliberadamente d | `src/builder/text/normalize.py:41-51 (canônica split_camel_case) vs src/builder/routing/mot` | ✖ sumiu (simbolo-sumiu) | NÃO unificar o motor agora — a divergência é medida e intencional. Risco é alguém 'consertar' importando split_camel_case no disambiguator: adicionar comentário |
| alta | performance | score_text_against_row: loop aninhado O(tokens_fonte × tokens_row) sobre markdown inteiro com duplicatas — 15x | `src/builder/extraction/entry_signals.py:31-42 (chamado por src/builder/routing/file_map.py` | ✅ vivo | Agrupar source_tokens com collections.Counter e multiplicar a contribuição pela contagem (equivalência numérica exata, apenas ordem de soma float muda — gates d |
| alta | performance | normalize_match_text sem cache: 52.256 chamadas para apenas 568 textos únicos numa única sonda (99% repetição, | `src/builder/text/normalize.py:8-38` | ✅ vivo | functools.lru_cache num wrapper (args são strings imutáveis + kwargs hasháveis; maxsize ~4096). Corta a sonda (compute_production_index) em ~metade e acelera to |
| alta | robustez | Loader único do motor engole artefato corrompido em nível DEBUG — motor vira no-op e gates medem contra contex | `src/builder/routing/motor/context.py:17-25` | ✅ vivo | Subir o log para warning (ou levantar exceção) quando o arquivo EXISTE mas é ilegível; em apply_anchor_engine, logar warning quando ctx.blocks vier vazio com o  |
| alta | robustez | code_curation.json corrompido é silenciosamente ZERADO e reescrito — perda permanente de toda a curadoria Gemi | `src/builder/core/code_summarization.py:328-335` | ✅ vivo | write_code_curation deve usar o padrão atômico tmp+os.replace (já existe em helpers.write_text e save_material_curation); load_code_curation deve logar warning  |
| alta | robustez | set_block_override com .timeline_curation.json corrompido apaga TODOS os overrides manuais ao gravar um único  | `src/builder/timeline/curation.py:94-100` | ✅ vivo | Em parse-failure com arquivo presente: recusar a gravação (levantar erro para a UI) ou preservar o corrompido como .corrupt e logar warning; trocar o write por  |
| alta | robustez | Guard 'unidade nunca encolhe em silêncio' é derrotado por índice corrompido — o gate passa exatamente no cenár | `src/builder/ops/pedagogical_regeneration.py:304-313` | ◐ inconclusivo | Distinguir 'índice ausente' de 'índice presente mas ilegível': no segundo caso, logar warning e tratar como encolhimento não-autorizado (levantar UnitsShrinkErr |
| alta | robustez | manifest.json gravado com write_text cru (não-atômico) e OSError engolido — bypass do writer canônico atômico | `src/builder/timeline/index.py:1552-1556` | ✅ vivo | Substituir os dois writes por write_json_manifest/write_text de src/utils/helpers.py e logar warning no except em vez de pass. |
| media | campos-mortos | MORTE-CERTA: S4 (TOOL_BOOST/TOOL_PENALTY/TOOL_TOKENS) em thresholds.py | `src/builder/routing/thresholds.py:85-87` | ✖ sumiu (simbolo-sumiu) | Deletar TOOL_BOOST/TOOL_PENALTY/TOOL_TOKENS junto com o scorer. NÃO tocar no resto de thresholds.py (confidence_band, relative_margin_confidence, METHOD_CAPS, I |
| media | campos-mortos | CIRURGIA: aliases de engine.py e facade/teaching_timeline.py que só existem para o funil | `src/builder/engine.py:52-58,2144-2154,2303-2326,2472-2478 + src/builder/facade/teaching_ti` | ✅ vivo | Podar os aliases dos condenados no cutover; manter os de score_card_evidence (compartilhado com cronograma_health). É a parte 'mecânica' da cirurgia — quebra im |
| media | campos-mortos | temporal_block_method: escrito pelos DOIS motores, lido por ninguém em prod | `src/builder/routing/motor/apply.py:46` | ✅ vivo | Ou exibir method no dashboard/dialogs junto com band/provider (auditabilidade da decisão), ou remover de _write_temporal e do FileEntry; hoje só round-tripa. |
| media | codigo-morto | 5 métodos-fachada mortos em RepoBuilder (engine.py) + imports aliased que só eles usam | `src/builder/engine.py:1845,1870,2006,2108,2111` | ✅ vivo | Deletar os 5 métodos wrapper. Junto morrem os imports aliased usados só por eles: engine.py:130 (_pdf_pipeline_log_backend_result), :165 (_ops_derive_active_uni |
| media | duplicacao | _tokens byte-idêntica em card_block e block_identity; _block_topic_tokens quase-gêmea diverge no primeiro camp | `src/builder/timeline/card_block.py:30-31 e src/builder/timeline/block_identity.py:40-41; t` | ✅ vivo | Unificar _tokens (importar de block_identity ou mover pra text/, mudança de comportamento zero). NÃO fundir _block_topic_tokens: a divergência primary_topic_lab |
| media | duplicacao | Família 'tokens >=4 sobre normalize_match_text' reimplementada em 4 módulos apesar de signal_token_set existir | `src/builder/text/normalize.py:54-67 (canônica) vs src/builder/routing/concept_resolver.py:` | ✅ vivo | Estender signal_token_set com parâmetro stopwords (default frozenset()) e migrar os 4 call-sites preservando cada conjunto exato — diff de comportamento zero po |
| media | duplicacao | code_summarization._normalize duplica a fonte única com diferença real de ASCII-ignore (deleta char em vez de  | `src/builder/core/code_summarization.py:137-143; consumida também por src/builder/core/refe` | ✅ vivo | Unificação quase-segura: delegar pra normalize_match_text(em_dash_to_hyphen=False, fix_typos=False) muda saída só em chars não-decomponíveis. Como alimenta atri |
| media | duplicacao | 5 convenções de stemming coexistem (prefixo-5, prefixo-6, prefixo-8 ×2 e corte de plural) sem módulo central d | `src/builder/extraction/content_taxonomy.py:226 e entry_signals.py:40 (prefixo 5); src/buil` | ◐ inconclusivo | NÃO unificar os comprimentos — cada um está amarrado a medição própria. O ganho barato é um stem(token, n) único em text/ chamado com o n local de cada site (el |
| media | duplicacao | topic_source (timeline block): escrito e re-serializado, sem leitor funcional | `src/builder/timeline/index.py:111` | ✅ vivo | Remover do payload serializado ou passar a exibir na UI de curadoria como explicação da origem do tópico; decidir antes do cutover para não migrar campo morto. |
| media | duplicacao | ImageCuratorPanel._preclassify: feature nunca ligada à UI | `src/ui/image_curator.py:1262` | ✅ vivo | Deletar o método, ou — se a feature era intencional — ligar a um botão. Como está, é peso morto que ainda importa classify_image. |
| media | duplicacao | ImageCuratorPanel._extract_latex_single: nunca chamado | `src/ui/image_curator.py:1426` | ✅ vivo | Deletar o método (há caminho vivo de extração em lote; este single-page ficou órfão). |
| media | mapa-legado | MORTE-CERTA: anchor_placement.py inteiro (camada temporal legada, 413 linhas) | `src/builder/routing/anchor_placement.py:1-413` | ✅ vivo | Deletar o arquivo inteiro no cutover + remover o elif em pedagogical_regeneration.py:534-539. Nada do motor novo importa deste módulo (verificado: imports do pa |
| media | mapa-legado | MORTE-CERTA: os 3 CONDENADOS oficiais do file_map — block_token_weights, score_entry_against_timeline_block, s | `src/builder/routing/file_map.py:894,938,1278` | ✅ vivo | Morte-certa no cutover, MAS com pré-requisito: primeiro operar cronograma_health.py (ver achado de cirurgia) — é o único consumidor fora do funil. |
| media | mapa-legado | MORTE-CERTA: cluster privado do scorer no file_map que só existe para servir os condenados | `src/builder/routing/file_map.py:696,812,830,878,1117,1155,1171,1236` | ✅ vivo | Morrem junto com os 3 condenados. EXCEÇÃO no mesmo arquivo: _score_block_date_match (:1090) e score_card_evidence_against_entry (:749) são importados pelo resol |
| media | mapa-legado | CIRURGIA: pedagogical_regeneration.py — 3 pontos no mesmo arquivo | `src/builder/ops/pedagogical_regeneration.py:518,532-539,227-250` | ✖ sumiu (simbolo-sumiu) | Cirurgia, não delete de arquivo: remover os dois gates e decidir o destino do D1 (provável delete — vira código morto funcional com resolver ON, mas exige mediç |
| media | mapa-legado | Família unit_match_*/subunit_match_*/unit_block_conflict/computed_subunit_slug: produtor ÚNICO é o legado cond | `src/builder/extraction/content_taxonomy.py:1424-1429` | ✖ sumiu (simbolo-sumiu) | Incluir no plano do cutover (campanha 3): ou o motor novo passa a produzir os sinais equivalentes (a UI depende deles para explicar o match), ou a UI ganha font |
| media | performance | backend_runtime.py (548 linhas) — maior módulo sem nenhum teste direto, exatamente onde mora a lógica de probi | `src/builder/runtime/backend_runtime.py:222-248 (e o arquivo inteiro)` | ✖ sumiu (simbolo-sumiu) | Testes unitários pequenos com importlib.import_module mockado: sucesso cacheia, falha não re-tenta (vai falhar hoje — é o bug), capabilities default do marker. |
| media | performance | incremental_build: _compact_manifest + write_json_manifest rodam POR ENTRY nova — O(N_novas × M_manifest) stat | `src/builder/ops/incremental_build.py:77-84 (loop), src/builder/artifacts/repo.py:596+204 (` | ✅ vivo | Manter o write_json_manifest por entry (durabilidade de resume é intencional), mas compactar UMA vez antes do loop — o filter_live/heal não muda entre iterações |
| media | performance | resolve_unit_block_tags (legado): collect_entry_unit_signals executado 3-5× por entry, cada vez re-normalizand | `src/builder/extraction/content_taxonomy.py:1136-1325 (loop por entry); call sites: src/bui` | ✅ vivo | Computar signals uma vez por entry em resolve_unit_block_tags e passar como parâmetro (os call sites já recebem via injeção de dependência); manter cômputo sepa |
| media | performance | auto_map_entry_unit reconstrói o índice de unidades (normalização + IDF) POR ENTRY; auto_map_entry_subtopic re | `src/builder/routing/file_map.py:382 e 145` | ✅ vivo | Indexar unidades uma vez fora do loop (aceitar índice pré-construído por parâmetro, como o topic_index já é aceito em auto_map_entry_unit) e passar topic_index  |
| media | performance | score_entry_against_timeline_block recomputa por par (entry×bloco): card-evidence pontuado 2× e extract_dates  | `src/builder/routing/file_map.py:873 + 1055 (card 2×); 1090-1104 + 1065 e src/builder/routi` | ✅ vivo | Cachear entry_tokens/entry_norm nos signals (uma vez por entry), tokens de card por bloco, datas do material por (entry, ano) e bounds por bloco. Se a soma dupl |
| media | robustez | Fallback 'preservar atribuições anteriores' converte índice corrompido em índice VAZIO sem log | `src/builder/timeline/index.py:1484-1491` | ✅ vivo | Logar warning com o erro de parse e considerar abortar a regeneração quando o cache existe mas é ilegível (o guard do achado 4 seria a segunda linha de defesa,  |
| media | robustez | load_card_block_map retorna {} em JSON corrompido sem log — curadoria card→bloco some do legado E do motor nov | `src/builder/timeline/card_block.py:149-156` | ✅ vivo | Logar warning quando o arquivo existe e o parse falha (uma linha resolve os dois motores, já que ambos passam por load_card_block_map). |
| media | testes | image_extraction: dict de diagnóstico gravado no manifest, lido só por testes | `src/builder/pdf/pdf_pipeline.py:219` | ✅ vivo | Mover a informação para builder.logs (que já registra o step extract_images em pdf_pipeline.py:225) e parar de persistir por entry. |
| media | testes | 2 testes dependem também do subjects.json do AppData do usuário e de UUIDs reais hardcoded | `tests/test_unit_matcher.py:158,164,181; src/models/core.py:333` | ✖ sumiu (simbolo-sumiu) | Injetar o SubjectProfile via fixture (construído do manifest.json do próprio repo) em vez de SubjectStore(); guardar os UUIDs esperados num arquivo gold version |
| baixa | codigo-morto | Migração de manual-review move arquivos mesmo quando o manifest não pôde ser lido — manifest fica apontando pa | `src/builder/artifacts/repo.py:1858-1913` | ◐ inconclusivo | Quando o manifest existe mas não pôde ser lido, pular a migração inteira (return 0) em vez de mover arquivos que o manifest não vai acompanhar. |
| baixa | codigo-morto | Lote de imports não usados em 6 arquivos menores | `src/builder/ops/lifecycle_ops.py:12` | ◐ inconclusivo | Remover os 8 nomes de import. Uma passada de ruff (F401) mantém isso limpo no CI. |
| baixa | duplicacao | semantic_config._normalize_text é normalize_match_text(keep="#+.-") reescrita à mão | `src/builder/core/semantic_config.py:74-80 vs src/builder/text/normalize.py:8-38` | ✅ vivo | Substituir o corpo por delegação à fonte única com keep="#+.-" e flags False — equivalência exata, unificação segura; manter o nome local como wrapper de 1 linh |
| baixa | duplicacao | tables_dir / table_detection_dir / advanced_asset_dir: consumidos apenas por réguas de GC/liveness | `src/builder/artifacts/repo.py:193-195` | ✅ vivo | Manter (a régua de GC depende deles); apenas não investir em enriquecê-los. |
| baixa | mapa-legado | TESTES mistos — morrem só em parte (exigem triagem, não delete cego) | `tests/test_resolve_unit_block_tags.py + tests/test_temporal_block_wire.py:1-9 + tests/test` | ✖ sumiu (arquivo-sumiu) | Triagem teste-a-teste no plano 3.4: separar os casos de unit/subunit/leitores (mantêm) dos casos de block-branch/anchor legado (morrem). NÃO usar 'arquivo impor |
| baixa | mapa-legado | posting_date: consumo real ainda não existe (só régua gold) | `src/builder/sources/moodle.py:459` | ✅ vivo | Nada a limpar — mas registrar que o campo só se paga quando A2 (consumo pela atribuição) for implementado; se A2 morrer no backlog, vira candidato. |
| baixa | performance | I/O repetido na regeneração: .timeline_index.json relido 4×, code_curation 3×, cada .md de entry relido 3-4× e | `src/builder/ops/pedagogical_regeneration.py:582, 613, 621, 630 (timeline), 516, 581, 612 (` | ✅ vivo | Carregar timeline_blocks e code_curation uma vez no início da regeneração em variáveis locais (o índice já está em memória como enriched_timeline_index na linha |

## Sérios SEM verdict (verifier morreu no limite — pendentes) (19)

| sev | dim | achado | onde | as-of 20/08 | ação |
|---|---|---|---|---|---|
| alta | mapa-legado | COMPARTILHADO (perigo): thresholds e entry_signals são a base de AMBOS os motores | `src/builder/routing/thresholds.py:20,71,109,118 + src/builder/extraction/entry_signals.py` | ✅ vivo | Intocáveis no cutover. Nota: METHOD_CAPS contém chaves que morrem com o funil ('review_rule', 'card+scorer', 'scorer_only') — attach_block_summary_fields usa `n |
| alta | mapa-legado | COMPARTILHADO (perigo): leitores resolve_effective_block / resolve_temporal_block e resolve_entry_manual_timel | `src/builder/routing/file_map.py:575,617,517` | ✅ vivo | Nenhum destes entra na lista de delete, apesar de morarem em file_map.py junto dos condenados. resolve_temporal_block continua necessário porque o motor NOVO es |
| media | campos-mortos | Duplicação documentada: computed_unit_slug/computed_block_id espelhados em auto_tags unit:/bloco: | `src/models/core.py:78-80` | ✅ vivo | Eleger o campo first-class como fonte única e tratar auto_tags unit:/bloco: como render derivado (não persistido), padronizando os leitores — reduz o risco de d |
| media | campos-mortos | tags (string legada) coexiste com manual_tags/auto_tags via migração permanente | `src/models/core.py:170-174` | ✅ vivo | Completar a migração: converter tags nos manifests existentes num passo único e remover os fallbacks de leitura — a lógica de migração em from_dict roda para se |
| media | codigo-morto | App.open_repo_dashboard_tab: método sem chamador | `src/ui/app.py:1084` | ✖ sumiu (simbolo-sumiu) | Deletar o método (app.py:1084-1087). |
| media | codigo-morto | App.open_file_map: método sem chamador | `src/ui/app.py:2750` | ✖ sumiu (simbolo-sumiu) | Deletar o método, ou ligar ao menu se a UX era desejada. |
| media | mapa-legado | MORTE-CERTA (parcial): rotas card→bloco de card_block.py — resolve_card_to_block e lookup_card_blocks | `src/builder/timeline/card_block.py:67,172,189` | ✅ vivo | Deletar resolve_card_to_block + lookup_card_blocks + lookup_card_assign_due (e load_card_block_map em content_taxonomy.py:1120 como caller). NÃO deletar o arqui |
| media | mapa-legado | CIRURGIA: cronograma_health.py re-scora com o scorer condenado — pré-requisito do delete | `src/builder/artifacts/cronograma_health.py:114-171,174-179` | ✅ vivo | Antes de deletar score_entry_against_timeline_block: trocar o fallback de _top_candidate_blocks para a janela do motor ou para o scoring do resolver (ou aceitar |
| media | mapa-legado | CIRURGIA: resolve_unit_block_tags NÃO morre inteiro — só o block-branch (linhas ~1207-1325) | `src/builder/extraction/content_taxonomy.py:1060-1432` | ✅ vivo | Plano de corte cirúrgico: substituir o miolo :1207-1325 pela chamada ao resolver (que já tem tier manual próprio, concept_resolver.py:271-284) mantendo unidade/ |
| media | mapa-legado | CIRURGIA: scripts que executam o funil diretamente — o harness de eval golden mede o motor ERRADO após o cutov | `scripts/eval_assignments.py:22-23,86-99 + scripts/retag_manifest.py:40-64 + scripts/compar` | ✖ sumiu (arquivo-sumiu) | Re-apontar eval_assignments.py e retag_manifest.py para o caminho do resolver ANTES do delete (senão o eval-gate do próprio cutover roda o legado). Arquivar com |
| media | mapa-legado | 11 arquivos de teste (~130 funções) amarrados ao caminho LEGADO condenado ao cutover da campanha 3 | `tests/test_file_map_unit_mapping.py (50 testes), tests/test_task2_uuid_migration.py (21), ` | ✖ sumiu (arquivo-sumiu) | No tracker da campanha 3, listar esses 11 arquivos como item de cutover: quais asserções migram pra contratos do motor novo e quais morrem com o legado. |
| media | performance | Índice corrompido durante code summarization envenena o cache: primary_block_id vazio persiste mesmo depois do | `src/builder/core/code_summarization.py:349-354` | ✅ vivo | Logar warning no parse-failure; não cachear (ou marcar para retry) summaries produzidos com blocks vazio quando o índice existe em disco. |
| media | performance | Motor novo (concept_resolver): pesos IDF dos blocos e vetores de conceito de blocos/unidades recomputados POR  | `src/builder/routing/concept_resolver.py:297, 338, 208, 226 (loop em src/builder/routing/re` | ✅ vivo | Como tool_tokens variam por entry (down-weight de formato), pré-computar por apply: frequency dos tokens por bloco + token-sets de blocos/unidades UMA vez, e de |
| media | robustez | Sidecar de votos LLM corrompido é silenciosamente substituído no read-merge-write — votos pagos descartados e  | `src/builder/routing/motor/llm_vote.py:71-77` | ✅ vivo | Logar warning em parse-failure com arquivo presente; opcionalmente preservar o arquivo corrompido como .corrupt antes do primeiro save (o write já é atômico, en |
| media | robustez | Uma entry malformada zera TODOS os índices derivados (EXAM/EXERCISE/ASSIGNMENT/CODE/WHITEBOARD) sem log | `src/builder/ops/pedagogical_regeneration.py:543-546` | ✅ vivo | Converter por-entry (try dentro do loop, pulando e logando a entry ruim) em vez de all-or-nothing silencioso. |
| media | robustez | Voter LLM opt-in degrada para None em qualquer falha sem nenhum log — rodada roda 'determinística' contra a in | `src/builder/ops/pedagogical_regeneration.py:74-75` | ◐ inconclusivo | Logar warning com o motivo dentro do except (uma linha), já que a ausência de voter muda o resultado da camada D9 que o usuário pediu. |
| media | testes | 22 testes dependem dos repos-tutor reais em ~/Documents/GitHub e viram skip silencioso fora desta máquina | `tests/test_caracterizacao_blocos_atual.py:40-51; tests/test_timeline_schema.py:109; tests/` | ✖ sumiu (simbolo-sumiu) | Aceitar como testes de corpo-vivo, mas torná-los visíveis: marcador pytest.mark.corpus + job dedicado que FALHA (não skipa) quando TUTOR_COURSES_DIR está setado |
| media | testes | 28 de 120 módulos de src/ sem nenhum import em tests/; destaque para sanitization (436 l) e image_markdown (34 | `src/builder/text/sanitization.py; src/builder/extraction/image_markdown.py; src/builder/co` | ✖ sumiu (simbolo-sumiu) | Priorizar os módulos de função pura (sanitization, image_markdown, url_markdown): testes de tabela baratos. UI (student_state_curator, maintenance_panel) só se  |
| media | testes | src/utils/helpers.py importa pdfplumber/pymupdf4llm/requests no topo do módulo — ~0.95s de import pago por TOD | `src/utils/helpers.py:12, 43-64` | ✖ sumiu (simbolo-sumiu) | Mover os três imports para dentro das funções que os usam (lazy import; os try/except já toleram ausência — viram função _get_pymupdf() memoizada). Corta ~1s do |

## Refutados (3)

- **MORTE-CERTA: helpers do block-branch do funil em content_taxonomy** — refutado: PARCIALMENTE REFUTADO. A alegação 'todos chamados exclusivamente pelo block-branch' é FALSA para 2 dos 5 helpers: _exam_code_from_block (content_taxonomy.py:1014) é importado por src/builder/artifacts/temporal_context.py:11 e chamado em temporal_context.py:47 (_tipo_label → build_temporal_context_ro
- **TESTES que morrem inteiros com o legado (verificados por import/símbolo)** — refutado: PARCIALMENTE REFUTADO. Confirmado para 3/5: _best_instructional_block_fallback (content_taxonomy.py:1271,1313), _card_scoped_block (:1269) e review_list_block_for_entry (:1261) têm callers exclusivamente no block-branch de resolve_unit_block_tags e morrem com ele (testes que morrem junto: tests/test
- **Um único teste consome 64% do tempo da suíte (45.88s de 71.74s) por importar docling de verdade** — refutado: REFUTADO na alegação central, com resíduo válido mais estreito. A alegação "sem asserções próprias, só exercitado indiretamente" é falsa: engine.py:411-421 e 389-429 mostram que os nomes do engine são ALIASES diretos dos objetos-função de backend_runtime (ex.: _should_force_ocr_for_marker = _backend

## Baixas (25) — registro

- [duplicacao] NFKD+remove-acentos+lower reimplementado em signals.py e em 3 scripts, com variante NFD divergente nos scripts de gold (`src/utils/helpers.py:441-446 (canônica norm_ascii_lower) vs src/builde`)
- [duplicacao] semantic_config._normalize_text é normalize_match_text(keep="#+.-") reescrita à mão (`src/builder/core/semantic_config.py:74-80 vs src/builder/text/normaliz`)
- [duplicacao] collapse_ws reinventado em script e inlined em 6 pontos do src apesar da helper canônica (`src/utils/helpers.py:259-260 (canônica) vs scripts/check_sarc_freshnes`)
- [robustez] Wrapper 'fromisoformat seguro' copiado 3x com semânticas levemente diferentes (`src/builder/timeline/block_identity.py:55-62 vs src/builder/artifacts/`)
- [codigo-morto] Migração de manual-review move arquivos mesmo quando o manifest não pôde ser lido — manifest fica apontando para paths m (`src/builder/artifacts/repo.py:1858-1913`)
- [mapa-legado] TESTES que morrem inteiros com o legado (verificados por import/símbolo) (`tests/test_anchor_placement.py:1 + tests/test_block_scorer_signals.py:`)
- [mapa-legado] TESTES mistos — morrem só em parte (exigem triagem, não delete cego) (`tests/test_resolve_unit_block_tags.py + tests/test_temporal_block_wire`)
- [mapa-legado] posting_date: consumo real ainda não existe (só régua gold) (`src/builder/sources/moodle.py:459`)
- [duplicacao] Duplicação de identidade nos blocos: id posicional vs block_uuid (`src/builder/routing/motor/contracts.py:61`)
- [duplicacao] Duplicação unit_slug vs auto_unit_slug nos blocos (16/23 idênticos) (`src/builder/timeline/index.py:103`)
- [performance] period_label: cache derivado que é re-parseado por regex em vez de usar period_start/end (`src/builder/timeline/index.py:930`)
- [duplicacao] topics duplica topic_text em 25/35 blocos do dado real (`src/builder/timeline/index.py:864`)
- [duplicacao] tables_dir / table_detection_dir / advanced_asset_dir: consumidos apenas por réguas de GC/liveness (`src/builder/artifacts/repo.py:193-195`)
- [testes] 4 skips permanentes em toda execução: casos-chave só existem para o repo de IA (`tests/test_caracterizacao_blocos_atual.py:195; tests/_golden/ (só Inte`)
- [duplicacao] Teste redundante: test_unit_short_label duplicado testando a mesma função via re-export (`tests/test_timeline_sort.py:97 vs tests/test_unit_labels.py:10; src/ui`)
- [testes] test_core.py é um monolito de 5630 linhas com 344 testes de domínios não relacionados (`tests/test_core.py:1-5630`)
- [testes] sys.path.insert manual em 6+ arquivos de teste apesar de pythonpath=['.'] no pyproject (`tests/test_motor_stopwords_pt.py:4; tests/test_migrate_signals.py:3; t`)
- [performance] I/O repetido na regeneração: .timeline_index.json relido 4×, code_curation 3×, cada .md de entry relido 3-4× e md5 do co (`src/builder/ops/pedagogical_regeneration.py:582, 613, 621, 630 (timeli`)
- [duplicacao] mask_key: função aninhada definida e nunca usada (`src/ui/dialogs.py:4866`)
- [duplicacao] WindowProvider (Protocol) no motor NOVO: nunca usado em anotação ou código (`src/builder/routing/motor/contracts.py:76`)
- [codigo-morto] dialogs.py: imports não usados is_sarc_url e BackendSelector (`src/ui/dialogs.py:11,24`)
- [codigo-morto] image_curator.py: import não usado extract_page_number (`src/ui/image_curator.py:18`)
- [codigo-morto] timeline_dashboard.py: import não usado derive_block_status (`src/ui/timeline_dashboard.py:20`)
- [codigo-morto] card_block.py (legado): import não usado _POSITIONAL_RE (`src/builder/timeline/card_block.py:17`)
- [codigo-morto] Lote de imports não usados em 6 arquivos menores (`src/builder/ops/lifecycle_ops.py:12`)

## O que fazer com isto

1. **Não agir sobre "✖ sumiu"** — já resolvido pelo cutover/limpezas.
2. **"✅ vivo" da dimensão robustez (severidade alta)** são os candidatos de maior valor: corrupção
   silenciosa de manifest/índice/curadoria (write não-atômico, except engolido, fallback que zera).
   Merecem fix pequeno + teste cada.
3. **Duplicação de tokenizadores/stemmers/stopwords** (5 grupos): unificação mecânica onde o
   verifier provou byte-identidade; onde há divergência de design, documentar no local.
4. **Pendentes sem verdict**: re-verificar com modelo BARATO (sonnet) antes de agir.
5. Dimensões **testes** e **performance**: finders morreram — re-rodar com modelo barato.
