{
  "type": "object",
  "properties": {
    "erros": {
      "type": "array",
      "description": "Uma linha para CADA um dos 109 erros do CSV de entrada, na mesma ordem. Nenhum pode faltar.",
      "items": {
        "type": "object",
        "properties": {
          "curso": {"type": "string"},
          "entry_id": {"type": "string"},
          "gold": {"type": "string"},
          "cru": {"type": "string"},
          "code_gold": {"type": "string", "description": "code do slug gold na tabela de topicos; vazio se nao houver"},
          "code_cru": {"type": "string", "description": "code do slug cru na tabela de topicos; vazio se nao houver"},
          "relacao": {
            "type": "string",
            "enum": ["pai_vence_filho", "filho_vence_pai", "irmaos_subtopico", "mesma_unidade_sem_parentesco",
                     "unidade_diferente", "predicao_vazia", "gold_vazio", "sem_codigo", "slug_ausente"]
          },
          "cru_e_ancestral_de_extra": {"type": "boolean"},
          "justificativa": {"type": "string", "description": "Uma frase, citando os dois codes."}
        },
        "required": ["curso", "entry_id", "gold", "cru", "code_gold", "code_cru", "relacao", "cru_e_ancestral_de_extra", "justificativa"]
      }
    },
    "totais": {
      "type": "object",
      "properties": {
        "pai_vence_filho": {"type": "integer"},
        "filho_vence_pai": {"type": "integer"},
        "irmaos_subtopico": {"type": "integer"},
        "mesma_unidade_sem_parentesco": {"type": "integer"},
        "unidade_diferente": {"type": "integer"},
        "predicao_vazia": {"type": "integer"},
        "gold_vazio": {"type": "integer"},
        "sem_codigo": {"type": "integer"},
        "slug_ausente": {"type": "integer"}
      },
      "required": ["pai_vence_filho", "filho_vence_pai", "irmaos_subtopico", "mesma_unidade_sem_parentesco",
                   "unidade_diferente", "predicao_vazia", "gold_vazio", "sem_codigo", "slug_ausente"]
    },
    "pai_vence_filho_por_curso": {
      "type": "object",
      "description": "curso -> quantidade de pai_vence_filho",
      "additionalProperties": {"type": "integer"}
    },
    "observacoes": {"type": "string", "description": "Anomalias de dados encontradas (code estranho, slug que nao casa). Duas frases no maximo."}
  },
  "required": ["erros", "totais", "pai_vence_filho_por_curso", "observacoes"]
}
