# Copie para .env na raiz do projeto. Carregado em os.environ no startup
# (src/utils/helpers.py) sem sobrescrever variáveis já definidas no OS.
GEMINI_API_KEY='Gemini API Key'
DATALAB_API_KEY='Data Lab API Key'
DATALAB_BASE_URL=https://www.datalab.to

# Moodle NÃO se configura aqui: o token é salvo pela GUI em moddle/.env
# (fonte única — manter cópia neste .env da raiz faria o valor stale vencer
# o token renovado pela GUI, silenciosamente).
